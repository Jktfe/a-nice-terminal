{
  "name": "@jktfe/remoteant",
  "version": "0.1.0",
  "description": "Personal MCP gateway — local stdio adapter for antchat-mac",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "bin": {
    "remoteant": "dist/cli.js"
  },
  "scripts": {
    "build": "bun build src/cli.ts --target=node --outfile dist/cli.js --define '__BUILD_GIT_SHA__=\\\"'$(git rev-parse --short HEAD)'\\\"'",
    "build:dev": "bun build src/cli.ts --target=node --outfile dist/cli.js --define '__BUILD_GIT_SHA__=\\\"dev\\\"'",
    "test": "../../node_modules/.bin/vitest run --config vitest.config.ts"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@types/node": "^20.12.0"
  }
}
