# ANT vNext Capability Ledger

Date: 2026-05-11
Owner: @evolveantcodex

Rule: no capability disappears silently. It can be redesigned, deduped,
deferred, or rejected with a reason, but it cannot vanish.

## Status Values

| Status | Meaning |
|---|---|
| KEEP | Rebuild the capability with the same behavior. |
| CHANGE | Rebuild the capability with different UX or architecture. |
| DEDUPE | Merge the capability into another surface. |
| DEFER | Keep it in scope, but not in the current phase. |
| REJECT | Remove it with a written reason. |
| UNKNOWN | Audit still required. |

## Source Buckets

| Bucket | Source | Treatment |
|---|---|---|
| V5 atlas | `antv5-wireframes.pen` | Binding product checklist. |
| Audit blockers | GLM/Kimi/Codex/Claude v5 audit | Must be represented in a board or ledger row. |
| Current ANT | `/Users/jamesking/CascadeProjects/a-nice-terminal` | Evidence only. Audit before copying. |
| External references | Product references such as Claude Code agent view | Benchmark patterns, not scope pivots. |

## M0 Coverage Rows

| Capability | Source | Initial status | Owner | Notes |
|---|---|---:|---|---|
| Start a chatroom | v5 chat/room lane | CHANGE | Claude | First vertical slice starts here, not from terminal. |
| Invite existing agent | v5 chat/room lane | CHANGE | Claude | Must include existing and new agent paths. |
| Invite new agent | v5 chat/room lane | CHANGE | Claude | Must show launch, handle, and room context. |
| Remote invite to room | v5 system lane | CHANGE | Codex | Keep security boundary explicit. |
| Participants list | v5 chat/room lane | CHANGE | Claude | Participants talk; features serve. |
| Change participant handle | v5 chat/room lane | CHANGE | Claude | Needs visible rename history. |
| Focus mode | v5 chat/room lane | CHANGE | Claude | Must not hide accountability. |
| Link discussions | v5 chat/room lane | DEDUPE | Claude | Discussion is a room facet, not a separate destination. |
| Artefact rail | v5 chat/room lane | CHANGE | Claude | Decks, docs, sheets, files, and sites share the rail. |
| Plans and tasks | v5 system lane | CHANGE | Codex | Plan is scoreboard, tasks are ownership and review. |
| DAG task dependencies | v5 system lane | CHANGE | Codex | Implemented a first usable DAG slice on the plan Gantt surface: persisted task dependency edges, cycle prevention, safe add/remove controls, and rendered graph links. |
| Shared docs | v5 chat/room lane | CHANGE | Claude | Room-facing durable synthesis. |
| Memory recall | v5 chat/room lane | CHANGE | Claude | Searchable context, not a dumping ground. |
| Navigate to other chats | v5 chat/room lane | CHANGE | Claude | Command palette and room switcher. |
| Upload pictures and files | audit blockers | CHANGE | Claude | Include drag/drop, picker, errors, policy, and mobile. |
| Break context | v5 chat/room lane | CHANGE | Claude | Breaks are first-class context boundaries. |
| Rename session | audit blockers | CHANGE | Codex | Session PATCH and visible rename history. |
| Search messages | v5 chat/room lane | CHANGE | Claude | Per-room first, global search later if productized. |
| Linked terminal | v5 terminal lane | CHANGE | Codex | Show linked chat, ANT terminal, and Raw terminal. |
| Bring agent up to speed | v5 terminal lane | CHANGE | Codex | Context packet must be inspectable. |
| Change folder | v5 terminal lane | CHANGE | Codex | Easy `cd` without hiding command truth. |
| Spawn agent in another terminal | v5 terminal lane | CHANGE | Codex | Human approves crossing terminal boundary. |
| SSH / local tmux toggle | v5 terminal lane | CHANGE | Codex | Same UX, different runtime boundary. |
| WezTerm / chat toggle | v5 terminal lane | CHANGE | Codex | Confirm exact product name during audit. |
| Keyboard shortcuts | v5 system lane | CHANGE | Codex | Discoverable and editable. |
| Grid / multi-window view | v5 system lane | CHANGE | Codex | Must be recoverable if layout breaks. |
| Message popups | audit blockers | CHANGE | Codex | Toasts and notification settings. |
| Replies | v5 chat/room lane | CHANGE | Claude | Reply polish: sender, quote, auto-mention. |
| Interviews | v5 interview lane | CHANGE | Claude | Ask for context, options, chat escalation. |
| Play / pause / speaker mode | v5 interview lane | CHANGE | Claude | Voice state visible and controllable. |
| Read receipts | audit blockers | CHANGE | Claude | Zero-token receipt model. |
| Reactions | audit blockers | CHANGE | Claude | Thumbs up/down plus mobile target. |
| Typing indicator | audit blockers | CHANGE | Claude | Stale timeout visible. |
| Draft persistence | audit blockers | CHANGE | Claude | Never lose typed text. |
| Consent grants | audit blockers | CHANGE | Codex | Lane A implemented general room grants with topic/source/duration/max-answer gate, audit trail, and ask-answer enforcement. |
| Archive recovery surface | audit blockers | CHANGE | Codex | Lane A implemented `/safety` recovery surface for archived rooms/plans plus explicit soft-delete boundaries. |
| Agent telemetry | audit blockers | KEEP | Codex | User-facing status, not debug soup. Wired up agent status (idle/thinking/working/response-required) directly to SQLite terminals + chat_agent_status_events tables, bypassing fingerprinting! |
| Sheets artefacts | audit blockers | CHANGE | Codex | Trust model and audit rail. |
| Plan event editing | audit blockers | CHANGE | Codex | Inline or modal, but explicit. |
| Agent session overview | Claude Code agent view, 2026-05-11 | KEEP | Codex | Borrowed good parts: status pills in Svelte room footers + fleet grid wired to SQLite. Bypasses stdout fingerprinting via direct script-based state updates. |
| Multi-model routing and cost savings | Current ANT agent mix and James product direction | CHANGE | Codex | Different models behave differently and cost differently. vNext must make Claude, Codex, Gemini, Qwen, Copilot, Pi, GLM, Kimi, and future agents first-class participants instead of forcing one vendor path. |
| Model routing preferences | Router-not-viewer stance | CHANGE | Claude/Codex | Users need defaults by task type plus override controls. Agent rows need model, cost tier, and tokens consumed this session. |
| Session tracker agent | James product direction | CHANGE | Codex | A cheap always-on tracker can watch sessions, cost, stale working states, decisions, and completions, then escalate only the prepared item. |
| Room read authorization | P0 home-server leak, 2026-05-21 | CHANGE | Codex | Implemented fail-closed room list/message/ask read gates: no anonymous room list, bearer/browser-session callers see only rooms their resolved identity can read. |
| Agent family access scope | JWPK room rule, 2026-05-21 | CHANGE | Codex | Implemented owner-family expansion so a user's agents can share that user's rooms without giving another user's agents broad access. |
| Accounts bearer home-server cache | Mac app 0.1.8 auth path, 2026-05-21 | CHANGE | Codex | Added shared accounts bearer introspection and local cache so Mac bearer reads/writes/license refresh do not re-hit accounts after the first successful resolve. |
| Plan attach-room CLI | RoomPlansPanel CLI gap, 2026-05-21 | CHANGE | Codex | Added the missing `ant plan attach-room <plan_id> <room_id>` command path so documented plan-room attachment has a working CLI surface. |
| Heads-down claim UX | Room-mode design contract, 2026-05-21 | CHANGE | Codex | Threaded persisted room mode into the room view, changed claim controls to use roster member kind instead of a hard-coded agent handle prefix, and added roster-order markers plus live claim countdowns. |
| Realtime and plan-room read gates | P0 home-server leak, 2026-05-21 | CHANGE | Codex | SSE room events and plan-room links now use the same resolved read gate as room messages; anonymous callers fail closed and authorized callers only see readable rooms. |
| CLI agent read auth | P0 home-server auth regression, 2026-05-21 | CHANGE | Codex | Room-scoped CLI GETs now send pidChain for registered-terminal reads and can mint the same browser-session cookie used by writes when a read gate returns 401, preserving agent tail/router/message reads after fail-closed route auth. |
| Agent status poller budget | Home-server responsiveness, 2026-05-21 | CHANGE | Codex | Bounded synchronous tmux/fingerprint subprocess work per tick and shortened subprocess timeouts so background status refresh cannot monopolize the Node event loop. |
| Browser-session mint auth | P0 home-server auth bypass, 2026-05-22 | CHANGE | Codex | Browser-session minting now requires bearer, existing browser-session, or registered pidChain room access, and rejects arbitrary/spoofed authorHandle requests unless the caller is the same owner family or has human-consent authorization. |
| Planning-mode signal | Speed Pact T14, 2026-05-22 | CHANGE | Codex | Added `ant status planning` / `ant status idle` so agents can push the existing thinking/idle icon state from their PID identity and optionally post the planning notice into a room. |
| Ask answer room receipts | Speed Pact T15, 2026-05-22 | CHANGE | Codex | Ask answers now create a system message in the originating room and use the normal terminal fanout + SSE message path so agents see the answer without polling the asks API. |
| Ask answer seen-by receipts | Speed Pact T16, 2026-05-22 | CHANGE | Codex | Answered-ask system receipts now render the existing message read indicator, reusing terminal fanout read marks and `message_read` SSE events instead of adding a separate ask-specific receipt system. |
| Ask defer contract | antios full-wiring goal, 2026-05-27 | CHANGE | Codex | Added a real `deferred` ask state plus `POST /api/asks/:askId/defer`. Deferred asks remain visible and response-required instead of being faked through the answer route, so native clients can expose a usable Defer button. |
| Status-line install pilot | Speed Pact T17, 2026-05-22 | CHANGE | Codex | Added `ant status install-line --cli qwen-cli` as the first idempotent status-line installer: it backs up Qwen's statusline script, preserves visible Qwen status text, and emits ANT-canonical state JSON under `~/.ant/state/qwen-cli/`. |
| Status-line room invite | Speed Pact T17 Phase 2, 2026-05-22 | CHANGE | Codex | Added a room footer action and `/api/chat-rooms/:roomId/status-line-invite` endpoint that posts one system invite and fans it out through the existing room message path so agents can install the proven qwen-cli status-line shape. |
| Stage deck focus reporting | stage-primitive-v1 M-Viewer, 2026-05-22 | CHANGE | Codex | Added a narrow `/api/decks/:deckId/stage-focus` write path and existing deck-viewer hook so slide changes publish `stage_focus` evidence into the Stage projection and fan out as normal room receipts. No new deck shell or Stage table. |
| Stage agent CLI focus controls | stage-primitive-v1 M-Viewer DEBUG, 2026-05-22 | CHANGE | Codex | Added `ant stage focus` and `ant stage current` over the same Stage focus endpoint so agents can publish/read deck focus with pidChain auth instead of relying on browser-only deck interaction. |
| Stage voice narration | stage-primitive-v1 M-Voice DEBUG, 2026-05-22 | CHANGE | Codex | Deck Stage voice now uses speaker notes before slide body, reads provider/autoplay settings from the voice availability endpoint, supports play/pause/resume/stop, pauses on slide changes, and avoids silent browser fallback unless the Stage voice provider is explicitly set to browser. |
| Stage voice memory contract | JWPK room memory directive, 2026-05-22 | CHANGE | Codex | Room-surviving lessons should be Markdown notes in the user-selected knowledge directory (JWPK: ObsidiANT vault), with room-memory rows storing recallable links and the room side panel showing those links. Hidden key/value rows alone are not enough. |
| Validation v1 policy preset + scoring | validation-feature-v1 M-JKsRule/M-Score, 2026-05-22 | CHANGE | Codex | Added JK's rule as a reusable verification-policy preset, a premium-gated preset seed endpoint, and pointer-based claim scoring. This intentionally reuses external artefact pointers instead of adding a document parser. |
| Validation claim extractor | validation-feature-v1 M-Extractor, 2026-05-22 | CHANGE | Codex | Added a thin extractor boundary that turns external-tool API fragments into stable `ValidationClaimPointer[]` records with source pointers and simple claim kinds. It does not parse Docs/Notion/Sheets/PDF files or replace those tools. |
| Validation v1 orchestration planner | validation-feature-v1 M-Orchestrator, 2026-05-22 | CHANGE | Codex | Added a side-effect-free planner that maps validation claim pointers plus policy requirements onto existing transport primitives: heads-down agent work, human interviews, artefact checks, and context-summary checks. No new transport or parser. |
| Validation markdown/doc extractor | validation-feature-v1 M-Extractor Markdown slice, 2026-05-22 | CHANGE | Codex | Added the first real source adapter for markdown/doc bodies: paragraphs, list items, and table data rows become line-pointed validation claim pointers while frontmatter, code fences, headings, quotes, and table headers are ignored. |
| Validation markdown file pilot | validation-feature-v1 M-Extractor local-file pilot, 2026-05-22 | CHANGE | Codex | Added a one-file local markdown entrypoint so an ObsidiANT markdown document can be read and passed through the markdown/doc extractor with stable file or obsidian source pointers. No crawler, DB, API route, or non-markdown parser. |
| Stage deck pause-context broadcast | stage-live-edit γ2, 2026-05-22 | CHANGE | Claude | Added `POST /api/decks/:deckId/stage-pause-context` so when TTS pauses on a slide the local PauseSnapshot is persisted as a `stage_pause_context` evidence entry on a stage-scoped plan and SSE-broadcast to the room. Schema labels derived fields with the `estimated_` prefix (per codex schema review) so claim-anchor consumers don't over-trust the offset. No artefact mutation. δ feedback submission and ε alternative-generation are downstream slices. |
| Stage live feedback alternatives | stage-live-edit δ/ε-light, 2026-05-23 | CHANGE | Codex | Feedback submitted from the deck viewer now creates a room message, doc artefact, proposal evidence task, and stage alternative plan events. This keeps the source deck immutable while giving agents and humans an immediate Version B surface to inspect or adopt. |
| Stage validation preview | stage-validation UX v1, 2026-05-24 | CHANGE | Claude/Codex | Added a deck-viewer "Show validation" toggle, public validation-lens picker, and numbered claim preview derived from the currently displayed slide or alternative. The v1 preview is deliberately heuristic and marks claims unverified until the follow-up verifier-run overlay is wired. |
| Validation lens over artefacts | validation-feature-v1 Stage integration, 2026-05-23 | CHANGE | Codex | Added `POST /api/artefacts/:artefactId/validate` and an artefact-page validation panel so a stored markdown doc/deck can be scored through a selected policy lens. The route uses room read gates, keeps the artefact immutable, and returns claim anchors, score, and verifier gaps instead of treating validation as one global status field. |
| Validation verifier work items | validation-feature-v1 orchestration Phase 1, 2026-05-23 | CHANGE | Codex | The artefact validation route can now create idempotent room tasks for missing verifier slots. Each task carries the stable claim id, quote, source pointer, verifier kind, and lens requirement, making trust work claimable without marking any claim as verified. |
| Validation verifier routing | validation-feature-v1 orchestration Phase 2, 2026-05-23 | CHANGE | Codex | Validation work creation now respects supplied orchestration participants: ready verifier slots become assigned room tasks, while missing slots remain unassigned claimable work. This turns validation from a gap report into routed work without trusting claims before evidence lands. |
| Validation run-backed scoring | validation-feature-v1 evidence consumption Phase 1, 2026-05-23 | CHANGE | Codex | Artefact validation now folds completed `validation_runs` for the active lens into claim checks before scoring. Pending/running runs do not count, invalid run metadata is ignored, and claims only move toward trusted when persisted verifier evidence exists. |
| Validation task-to-run write path | validation-feature-v1 evidence consumption Phase 2, 2026-05-23 | CHANGE | Codex | Added a room-mutation-gated `/api/tasks/:taskId/validation-run` endpoint that turns a completed validation verifier task into a deterministic validation run. It parses the claim/lens/verifier metadata already carried by the task and refuses unfinished tasks, so verifier work becomes evidence without letting open tasks mark claims trusted. |
| Validation verifier task UI | validation-feature-v1 evidence consumption Phase 3, 2026-05-23 | CHANGE | Codex | Completed validation verifier tasks now show a pass/fail evidence form in the existing task detail panel. The UI posts to the task-to-run endpoint and only appears when the task description carries claim/lens/verifier metadata, keeping room memory and validation badge rendering separate from verifier evidence submission. |
| MCP server lifecycle guard | first-day-user optimisation sweep, 2026-05-25 | CHANGE | Codex | `@jktfe/mcp-server-ant` now exits when stdio ends/closes and reaps older same-parent sibling MCP server processes on startup. This bounds the observed stale-process leak without adding an idle timeout that could break legitimate long-running MCP sessions. |

## Audit Backlog

- Full current ANT route map.
- Current database schema copy/change/reject table.
- Current CLI command copy/change/reject table.
- Current terminal runtime copy/change/reject table.
- Current auth/token/grants copy/change/reject table.
| Stage alternative generation processor | stage-live-edit ε1, 2026-05-23 | CHANGE | Kimi | `stageAlternativeProcessor.ts` — finds unprocessed `stage_feedback` plan events, reads downstream deck slides, generates 1-3 rewritten slide proposals as `stage_alternative` evidence entries on new `plan_decision` events. Rule-based negative-sentiment detection (⚠️ title prefix + speaker note append). LLM swap deferred to future slice. Reuses existing plan_events + deckStore + EvidenceRef primitives. No new tables. |
| Stage alternative trigger endpoint | stage-live-edit ε2, 2026-05-23 | CHANGE | Kimi | `POST /api/decks/:deckId/process-alternatives` — idempotent endpoint that runs the stageAlternativeProcessor for a given deck and returns count of alternatives generated. Same auth gate as stage-feedback. Pure event-to-event processor trigger. |
| Stage feedback end-to-end wiring | stage-live-edit ε3, 2026-05-23 | CHANGE | Kimi | Deck viewer `submitFeedback()` now POSTs to `stage-feedback`, then immediately POSTs to `process-alternatives`, surfacing the generated count in the UI notice. Completes the pause → feedback → alternative generation loop. |
| Stage presenter password auth | stage-live-edit auth fix, 2026-05-23 | CHANGE | Codex | Stage presenter write endpoints now accept the deck share password for that deck only (`stage-pause-context`, `stage-feedback`, `process-alternatives`) while preserving admin/room mutation auth for normal callers. The deck viewer forwards the URL password on presenter POSTs, and the feedback button label is the simpler `Submit`. |
| Validation lens schema | validation-feature-v1 lens schema, 2026-05-23 | CHANGE | Kimi | `validation_schemas` + `validation_runs` SQLite tables with idempotent migration in db.ts. `validationLensStore.ts` provides CRUD, archive, seed (POC/FCA/Investment Memo), and run lifecycle (create/complete). Per-user lens model — same claim gets different badges per active schema. |
| Validation schemas API | validation-feature-v1 lens API, 2026-05-23 | CHANGE | Kimi | `GET /api/validation-schemas` — returns seeded lenses with idempotent seed on each call. |
| Room-memory file primitive | room-memory-v1 auth fix, 2026-05-23 | CHANGE | Kimi/Codex | `roomMemoryStore.ts` — file-based memory primitive writing Markdown files to `OBSIDIAN_VAULT_PATH/room-memories/<memoryID>.md`. Frontmatter includes memory_id, created_at, linked_rooms[], tags[]. `GET /api/rooms/:roomId/memories` now requires room read access and `POST /api/rooms/:roomId/memories` requires room mutation auth, closing the unauthenticated vault-read and memory-poisoning path. Replaces hidden key/value rows with durable MD artefacts in the user's chosen vault. |
| Validation badge rendering | validation-feature-v1 badge UI, 2026-05-23 | CHANGE | Kimi/Codex | `ValidationBadge.svelte` — lens-aware status badge that fetches latest validation run from `GET /api/validation-runs?taskId=` so badge reads inherit the validation task's room boundary. Compact mode (colored dot) for `RoomTasksPanel`, full mode for `TaskDetailPanel`. Status colors: pending (#9ca3af), running (#3b82f6), passed (#22c55e), failed (#ef4444), waived (#a855f7). |
| Validation runs API | validation-feature-v1 runs read/auth fix, 2026-05-23 | CHANGE | Kimi/Codex | `GET /api/validation-runs?taskId=` — resolves the validation task, applies that task's room read gate, then returns runs for the claim anchor carried by the task metadata. Bare `claimAnchor` reads are rejected so validation result JSON cannot leak outside the room boundary. Used by ValidationBadge component. |
| Tasks API auth containment | speed-matters security sweep, 2026-05-23 | CHANGE | Kimi/Codex | `/api/tasks` now fails closed: room-filtered GET requires room read access, room-linked POST requires room mutation auth, and no-room GET/POST plus `includeDeleted=1` require admin-bearer. This closes the unauthenticated cross-room task-subject leak without designing a new standalone-task visibility model. |
| Task route auth containment | tasks-auth-containment 2026-05-23 | CHANGE | Kimi | `GET /api/tasks` — room-linked filtered by requireChatRoomReadAccess; no-room/admin-bearer only. `POST /api/tasks` — room-linked gated by requireChatRoomMutationAuth; no-room/admin-bearer only. `?includeDeleted=1` admin-bearer only. 9 auth-containment tests covering unauth rejection, admin success, and room-gate success. Prevents cross-room task data leakage. |
| Context break delete | Slice 4 room mechanics, 2026-05-23 | CHANGE | Codex | Added room-mutation-gated `DELETE /api/chat-rooms/:roomId/breaks/:breakId` for soft-deleting system-break rows. Agent context slicing now ignores deleted breaks when choosing the active boundary, while normal message delete remains author-owned and still refuses system messages. |
| Contracts distribution split | contract-pack-v1, 2026-05-23 | CHANGE | Claude/Codex | Added `scripts/sync-contracts.mjs` to classify private-vault contracts by `visibility: oss | premium | private`. OSS-visible contracts sync to `docs/contracts/`; premium contracts sync to the private antchat resource folder and are not exposed through a public server dump/list endpoint. Missing visibility fails closed. |
| Room contract binding | contract-binding-v1 2026-05-23 | CHANGE | Kimi | `chat_rooms.contract_id` column + `POST /api/chat-rooms/:roomId/contract` endpoint (requires room mutation auth). RoomNameHeader shows 📜 contract badge when a room is bound to a governance contract. Enables room-level protocol enforcement. |

## 2026-05-30 Ship Log

| Capability | Source | Status | Owner | Notes |
|---|---|---|---|---|
| PR-D tools catalog migration + ant audit verbs | plan `ant-substrate-v0.2-2026-05-29` milestones `pr-d-tools-catalog` + `pr-d2-import-skills`; JWPK msg_mjh7rgi3wa + msg_6gq9zczigb 2026-05-30 | CHANGE | speedyc | Replaces filesystem-loaded skill/MCP/CLI catalog with DB-row source of truth. Closes the "nifty leak" case JWPK surfaced: deleted skill stayed referenced because the catalog was the filesystem, not a queryable surface. Two new tables (additive — `caller_grants` from 2026-05-19 + `grants_shim` from PR #98 coexist for now, consolidation in v0.2 cut-over): `tools_catalog` (tool_id PK, tool_slug, kind CHECK(skill/mcp/cli-verb/hook/plugin/bridge), name, description, version, source_path, owner_org, min_tier CHECK(oss/premium/internal), added_at_ms, deprecated_at_ms, retired_at_ms, metadata_json; UNIQUE(tool_slug) WHERE retired_at_ms IS NULL so the same slug can be re-registered after retire) and `tool_grants_v02` (grant_id PK, grantee_handle, tool_id FK → tools_catalog, scope_kind CHECK(global/org/room/session), scope_id, granted_by_handle, granted_at_ms, revoked_at_ms, expires_at_ms, reason; (grantee_handle, tool_id, scope_kind, scope_id, revoked_at_ms) lookup index + (tool_id, revoked_at_ms) tool-side index). New store `src/lib/server/toolsCatalogStore.ts` (14 exports): `registerTool` (idempotent — re-register on active slug is a no-op), `deprecateTool` (soft, still usable), `retireTool` (hard, makes grants orphans), `listTools` (kind/owner_org/includeRetired filters), `findToolBySlug` / `findToolById`, `grantTool` (rejects retired-tool grants — the bug class the catalog exists to prevent), `revokeToolGrant`, `lookupActiveGrant` (respects expires_at_ms), `listGrantsForAgent` / `listGrantsForTool`, `listOrphanGrants` (active grants pointing at retired tools — the nifty-leak detector), `listOrphanedTools` (active tools with zero active grants), `listRevocationsSince`, `countActiveGrantsForTool`. Six new HTTP endpoints (admin-bearer on writes; open read): `POST /api/tools` register, `GET /api/tools` list with filters, `GET /api/tools/[tool_id]`, `DELETE /api/tools/[tool_id]` retire, `POST /api/tools/[tool_id]/deprecate`, `POST /api/tool-grants` grant, `DELETE /api/tool-grants` revoke, `GET /api/tools/audit` (audit=tools\|grants\|revocations\|orphans single-query surface for CLI). New CLI verbs `ant tools register \| deprecate \| retire \| list \| grant \| revoke \| import-skills` via `scripts/ant-cli-tools.mjs` wired into `scripts/ant-cli.mjs` dispatcher with `--admin-token` flag falling back to `ANT_ADMIN_TOKEN` env. New `ant audit tools \| grants \| revocations \| orphans` subverbs extend `scripts/ant-cli-audit.mjs` (pre-existing `ant audit permissions` untouched). `import-skills --dry-run|--commit [--source-path PATH]` is the one-shot filesystem-to-DB migration verb — scans `~/.claude/skills/` + repo-local `.claude/skills/` for `SKILL.md` files, parses the YAML frontmatter (tiny dialect parser — key: "value" only), and writes a `tools_catalog` row per skill with `kind='skill'`. Idempotent on re-run because the UNIQUE-on-slug-where-active index makes register a no-op for known slugs. 137 new tests green (39 store + 38 CRUD endpoint + 11 audit endpoint + 28 tools-CLI + 21 audit-CLI): register idempotency + retire/re-register recovery + grant-against-retired rejection + scope_id NULL handling + expires_at_ms expiry + orphan-grant detection + orphan-tool detection + revocations-since + import-skills dry-run + commit + frontmatter parser + 401/404/400/503 endpoint coverage. Regression sweep on adjacent stores green (`grantsShimStore` 11 + `reclaimRequestsStore` 25 + `permissionRequestsStore` 23 + `/api/grants` 14 + `/api/permission-requests` 28 + `/api/reclaim-requests` 25 + `terminalsStore` 20 + `roomMembershipsStore` 19 + `identityKeysStore` 17 = 182). Fixed two pre-existing missing-comma bugs in `db.ts` SCHEMA_DDL_STATEMENTS array (after the `idx_reclaim_requests_target` index and after the `device_revocations` view) that were template-literal-concatenating adjacent statements at compile time — required for any new store test to load the schema. Org-admin attestation on the write endpoints lands with the substrate-wide trust_pubkey lift queued in the plan; the row shape is forward-compatible (no schema change required). NOT modifying any file owned by sibling branches (PR-B `register/+server.ts`, v0.2 schema reshape `db.ts` v02_ tables, PR-C reclaim_requests, PR #98 grants_shim, PR #99 identity_keys, PR #105 permission_requests). Part of plan `ant-substrate-v0.2-2026-05-29` milestones `pr-d-tools-catalog` + `pr-d2-import-skills`. |

## 2026-05-23 Ship Log

| Capability | Source | Status | Owner | Notes |
|---|---|---|---|---|
| Stage B foundation: permission_requests + pending_actions tables + ant request CLI | plan `ant-substrate-v0.2-2026-05-29` milestone `p3-stage-b-permission-requests`, 2026-05-29 | CHANGE | Claude (speedyc) | Substrate primitives that make the "agent hits 403 → human sees a modal → one-click approve → agent retries seamlessly" flow possible. Two new tables (additive — no existing schema modifications): `permission_requests` (request_id PK, requester_handle, action, target_kind/target_id, reason, approver_handles_json snapshot, status enum pending/approved/denied/expired/superseded, decided_at/by/scope, resulting_grant_id, pending_action_id; pending-status partial index + (target_kind, target_id, status) index + requester-side index) and `pending_actions` (action_id PK, request_id FK to permission_requests, http_method/http_path/payload_json/headers_json, created_at/expires_at, replayed_at, replay_status enum pending/ready_for_replay/replayed_by_caller/expired/denied; partial expiry index). New store `src/lib/server/permissionRequestsStore.ts` (10 exports): `createPermissionRequest` atomic write of both rows in one transaction; `approveRequest` atomic flip-to-approved + grants_shim write + pending_action flip to ready_for_replay (with wall-clock check — already-expired pending_actions stay pending so the sweep marks them expired, no stale replay); `denyRequest` flips both rows; `expireRequest` idempotent on already-decided; `sweepExpiredPendingActions` indexed UPDATE returns counts; `getPermissionRequest` / `getPendingActionForRequest` / `markPendingActionReplayed` / `listPendingForApprover` (snapshot-match) / `listPendingForRequester` / list+reset test helpers. Default TTL 5 minutes (`DEFAULT_PENDING_ACTION_TTL_MS`, test override). Three new HTTP endpoints: `POST /api/permission-requests` (admin-bearer OR pidChain auth; resolves approvers via Stage A's `resolveApproversFor`; 400 on bad body / invalid targetKind / bad http_method / relative http_path / non-positive ttlMs / non-JSON; 401 on unauth; 201 with `{request, pendingAction?}`), `POST /api/permission-requests/[id]/approve` (admin-bearer bypass; non-admin must be in Stage A's `resolveApproversFor` snapshot of the target; 401 wins over 404 on unauth probes — no request-id leakage; 404 on missing; 409 on already-decided; 400 on invalid decisionScope; 403 with structured `permission_denied` payload on non-approver — system-scoped explicitly admin-only with not_org_admin reason; 200 with `{request, grant, replay: {ready, status, actionId}}`), `POST /api/permission-requests/[id]/deny` (mirror approver gate; 200 with `{request}`; 400 on non-string reason). Also added `GET /api/permission-requests/[id]` (requester OR approver OR admin-bearer read access) for the CLI poller surface and `GET /api/permission-requests?asApprover=1&pidChain=...` for inbox listing. New CLI verb `ant request approve|deny|list|show` via `scripts/ant-cli-request.mjs` wired into the dispatcher in `scripts/ant-cli.mjs` (--scope flag for approve, --reason for deny, --approver flag for list). New cron entry `permissionRequestsSweepBoot.ts` wired into `hooks.server.ts` boot path — 60s setInterval calling `sweepExpiredPendingActions`, boot-once via globalThis flag matching the cronJobTicker pattern. CLI-driven replay model (server signals ready_for_replay; CLI retries the original action and calls `markPendingActionReplayed` for the audit trail) — explicitly NOT server-side internal HTTP replay (simpler, easier to debug, zero infinite-replay-loop risk). 70 new tests across 6 files: `permissionRequestsStore.test.ts` (23 covering create with/without pending_action, atomic approve flips request+pending_action+grant, expired pending_action stays pending across approve, deny + flow, idempotent expire, sweep transitions, double-approve throws, list helpers, normalisation), `permissionRequestsSweepBoot.test.ts` (3 for tick + idempotent boot + error-swallowing), `/api/permission-requests/server.test.ts` (12 for POST happy/admin/pidchain/with-pending_action paths + 400/401/empty-approvers), `/api/permission-requests/[requestId]/approve/server.test.ts` (9 covering admin bypass, room-owner approve, non-approver 403 with structured payload, 401/404/409/400, system-scoped non-admin reject, scope honoured), `/api/permission-requests/[requestId]/deny/server.test.ts` (7 mirroring approve gate shape), `scripts/ant-cli-request.test.mjs` (16 covering all four subcommands + error surfacing + flag parsing). All 592 pre-existing tests still pass (regression sweep: 8 hooks.server + 18 permissionDeniedPayload + 11 grantsShimStore + Stage A endpoint + chat-room sweep). NOT modifying any file owned by sibling branches (PR-B `register/+server.ts`, v0.2 schema reshape `db.ts`, Part 4 identity tables, PTY hardening). Auto-call from chat-rooms 403 sites is deliberately NOT wired in this PR — Stage A's structured payload is the stable contract; a follow-up CLI `--auto-request` flag (or antos app) opts in to round-tripping the original action through the new endpoint. Modal routing for approvers stays deferred (depends on antos and antchat apps); this PR ships the substrate they will hook into. |
| Stage A: 403 PermissionDenied payload restructure | ratified PR shape `docs/concepts/` (in-flight) + plan `ant-substrate-v0.2-2026-05-29` milestones `p3-stage-a-403-payload` + `p3-stage-a-grant-cli`, 2026-05-29 | CHANGE | Claude (speedyc swarm spawn) | Permissions UX win: every 403 from `/api/chat-rooms/**` + `/api/grants` now returns a structured `permission_denied` block with `action`, `target_kind`, `target_id`, `target_display_name`, stable `reason` enum (`no_membership` / `room_closed` / `not_room_owner` / `not_org_admin` / `no_grant` / `identity_unresolved` / `tier_required` / `human_consent_required`), `approvers[]` with preferred-flag (room_owner / plan_owner / task_assignee / org_admin / task_creator), an `approve_command` string-equal to the CLI invocation that would grant it, and forward-compatible `approve_url` / `request_id` / `expires_at_ms`. New helper `src/lib/server/permissionDeniedPayload.ts` (builder + reason humaniser + runtime type guard), new approver resolver `src/lib/server/permissionApproverResolver.ts` (room→owner / plan→creator / task→assignee+plan_owner+creator fallback / org→admins / system→empty), new `grants_shim` table + `grantsShimStore.ts` (grantPermission / revokePermission / lookupActiveGrant + listAllGrantsForTests; row shape byte-identical to v0.2 `grants` so the Stage B migration is a row-copy). New `ant grant <handle> <action> --room|--plan|--task|--org|--system <id> [--once|--always-for-room|--always-for-agent] [--revoke]` CLI verb wired into the dispatcher via `ant-cli-grant-verb.mjs` (the standalone `ant-cli-grant.mjs` from the 2026-05-19 caller_grants slice is intentionally untouched — different surface, different endpoint). Backed by new `/api/grants` (POST + DELETE, admin-bearer OR pidChain auth). CLI renderer `ant-cli-permission-denied.mjs` emits 4-6 stderr lines (head + reason + approver + approve_command + optional URL + optional request_id) wired into `sendJson` / `sendJsonWithCookie` in `ant-cli-chat.mjs` AND `throwIfNotOk` in `ant-cli.mjs`. 403 throw sites rewritten: `chatRoomAuthGate.ts:admin_cannot_impersonate_human`, `chat-rooms/[roomId]/messages/+server.ts:human_impersonation_no_terminal` + `rejectMessageIdentity`, `chat-rooms/[roomId]/members/+server.ts:not-in-org`, `chat-rooms/[roomId]/mode/+server.ts:not-a-member`, `chat-rooms/[roomId]/responders/+server.ts:not-a-member`. Legacy `message` strings preserved on every rewrite so existing audit-of-impersonation tests + alerting filters + CLI wedge-hint matches continue to work; new consumers read the `permission_denied` block. 67 new tests across `permissionDeniedPayload.test.ts` (11) + `grantsShimStore.test.ts` (11) + `/api/grants/server.test.ts` (9) + `ant-cli-grant-verb.test.mjs` (13) + `ant-cli-permission-denied.test.mjs` (13) + `chat-rooms/[roomId]/messages/permission-denied-payload.test.ts` (1) + chat-room test sweep continues 548/548 green. Out of scope for Stage A (deferred to Stage B / Part 2): modal routing for approvers, `pending_actions` queue for auto-replay, `permission_requests` audit-trail rows, full owner/admin gate on the grant endpoint (Stage A trusts the CLI surface). |
| PR-C super-admin reclaim CLI + reclaim_requests table | substrate v0.2 plan ant-substrate-v0.2-2026-05-29, milestone pr-c-super-admin-reclaim, 2026-05-30 | CHANGE | speedyc | Replaces tonight's 4-hour raw-SQL identity-surgery forensic with a 2-line audited CLI primitive. `ant reclaim file` + `execute` + `deny` + `list` + `show`. New `reclaim_requests` table (additive; `reclaim_id`, `requester_handle`, `target_kind` CHECK(terminal/membership/identity/session), `target_id`, `reason`, `diagnostic_json`, `status` CHECK(pending/approved/executed/denied/expired), `created_at_ms`, approver + executor stamps, `resulting_actions_json`, `signed_payload`, `signature`). `signed_payload` is canonical JSON captured at create time; `signature` is NULL in Stage A (admin-bearer only) and populated when Part 4 trust_pubkey + org-admin attestation lifts ship — future tightening lands without schema change. New `src/lib/server/reclaimRequestsStore.ts` exports `createReclaimRequest` / `approveReclaim` / `executeReclaim` / `denyReclaim` / `expireReclaim` / `getReclaimRequest` / `listPendingReclaims`. `executeReclaim` is the load-bearing primitive: dispatches on `target_kind` — `terminal` (flip terminals.status -> 'archived' + soft-revoke any active room_memberships rows pointing at it), `membership` (soft-revoke one row), `identity` + `session` (NO-OP with explicit `noop_*_pending_v02` warning action until v0.2 tables live; status still flips to executed for the audit trail). `dryRun=true` returns the would-run actions without mutating rows or flipping status. All actions captured in `resulting_actions_json` so `ant reclaim show <id>` surfaces the full audit trail. Five new HTTP endpoints (all admin-bearer only in Stage A — widens when Part 4 lift ships): `POST /api/reclaim-requests` (file), `GET /api/reclaim-requests` (list pending), `GET /api/reclaim-requests/:id` (show), `POST /api/reclaim-requests/:id/execute` (dryRun + live), `POST /api/reclaim-requests/:id/deny` (mandatory reason). New `ant reclaim` CLI verb wired into `scripts/ant-cli.mjs` dispatcher with `--admin-token` flag falling back to `ANT_ADMIN_TOKEN` env. 72 new tests green (25 store + 25 endpoint + 22 CLI): create/approve/deny/expire state machine, dryRun-vs-live execute for all 4 target_kinds, unknown-target warnings, 401-wrong-bearer + 503-no-admin-token + 400-invalid-body + 404-unknown-id + 409-already-decided coverage. svelte-check unchanged (4 pre-existing errors in validation-summary only). Part of plan ant-substrate-v0.2-2026-05-29 milestone pr-c-super-admin-reclaim. |
| feat(v0.2): create all 11 v0.2 schema tables (additive migration, v02_ prefix) | JWPK /goal msg_5l76efhrxg + archive-and-ditch msg_jnh0l9idnb v4.1 2026-05-29; canonical spec `docs/concepts/ant-v02-identity-and-recovery.md` + FlowSpec canvas `e657713c-4556-4885-a4fc-70c0e3489a9e` | CHANGE | Claude (claudev4) | Additive schema migration for v0.2 — creates `v02_agents`, `v02_agent_trust_keys`, `v02_runtimes`, `v02_rooms`, `v02_memberships`, `v02_tool_grants`, `v02_permission_requests`, `v02_pending_actions`, `v02_reclaim_requests`, `v02_key_rotation_requests`, `v02_audit_events` (11 tables total). Old tables (`terminals`, `terminal_records`, `room_memberships`, `chat_room_members`, etc.) untouched until cut-over PR — both schemas coexist during migration window. Structural invariants (UNIQUE-WHERE-LIVE on runtimes + memberships, NO cached `fanout_target_runtime_id` column on memberships) enforced via SQLite constraints, not application code — dual-bind on runtime + roster/fanout split on memberships become constraint violations instead of silent drift. New `V02_SCHEMA_DDL_STATEMENTS` constant in `src/lib/server/db.ts` (570 LOC additive, no edits to existing tables). New `src/lib/server/v02-schema.test.ts` (16 tests, all passing): every v02_ table exists / both UNIQUE indexes reject the bad shape / `v02_memberships` PRAGMA confirms no fanout-cache column / `v02_agents.primary_trust_key_id` nullable + FK link verified / `v02_audit_events.entity_kind` CHECK covers all 11 entity types + 'system' / migration idempotent across two re-opens of the same on-disk DB. `bun run check` 0 errors 0 warnings (3604 files). No data backfill — JWPK archive-and-ditch decision per msg_jnh0l9idnb 2026-05-29. PR #100 ships a separate non-prefixed `reclaim_requests` table for immediate super-admin reclaim CLI; this PR's `v02_reclaim_requests` is the canonical v0.2 sibling — both coexist during migration window, reconciled by code at cut-over (not by schema rename). No server code reads from v02_ tables yet — that ships in a follow-up cut-over PR. FK references to out-of-scope entities (`orgs`, `cli_providers`, `tools`) are TEXT columns without REFERENCES clauses; FK constraints can be added via ALTER once those tables ship. |
| Substrate v0.2 Part 4 — identity_keys + identity_attestations + recovery_grants tables + `ant identity` CLI verbs | ratified canvas `/tmp/ant-identity-keys-multi-device-canvas-2026-05-29.md` (designed by @speedyc, JWPK greenlit 2026-05-29 ~21:45 BST) | NEW | Claude Opus 4.7 | Foundation for the v0.2 trust_pubkey auth path. Closes the "James-Stevenson laptop in car crash" recovery scenario at the substrate level. Multi-device keys per identity, three recovery tiers, paper-mnemonic mandate at identity creation. **Schema** (additive in `src/lib/server/db.ts`, no changes to `terminals` / `terminal_records` / `room_memberships`): `identities` (kind ∈ human/agent/bot/system; `paper_key_hash` column for Tier 3 fallback; UNIQUE INDEX on canonical_handle WHERE revoked_at_ms IS NULL); `identity_keys` (multi-per-identity, key_kind ∈ device/paper/escrow; revoked rows are terminal — new key for re-attested device = new key_id; partial index on active rows for hot-path lookup); `identity_attestations` (append-only signed log; attester_kind ∈ self/org-admin/paper-key/multi-admin records the recovery tier; canonical_payload stored for forensic replay); `recovery_grants` (Tier 2 + Tier 3 pending-approval queue; status ∈ pending/approved/denied/expired); plus a `device_revocations` SQL VIEW joining identity_keys + identity_attestations for fast admin queries. **Store** (`src/lib/server/identityKeysStore.ts`): ed25519 sign/verify via node:crypto (no external dep — `@noble/ed25519` would have added a runtime dep where Node has native support); 32-byte raw key bytes stored as base64; `createIdentity` / `mintIdentityKey` (atomic write of key + attestation in one transaction; `selfAttestForBootstrap` flag for the first-key chicken-and-egg case) / `revokeIdentityKey` (writes revocation attestation + flips revoked_at_ms — terminal, idempotent throw on double-revoke) / `listActiveKeys` / `lookupIdentityByPublicKey` (used by Stage B auth gate) / `verifyAttestation` (re-verifies stored signature against attester's public_key; returns `{valid, attesterKey}` for forensic-audit); recovery_grants `requestRecovery` / `approveRecovery` / `denyRecovery`; in-memory challenge-nonce store (5-min TTL) for the two-leg attest handshake. **HTTP endpoints** (admin-bearer for this slice; per-identity scoping ships in Stage B permissions): `POST /api/identity/attest-challenge` validates target identity + optional attester key, issues a 32-byte nonce; `POST /api/identity/attest-device` consumes the nonce, verifies the supplied signature against the attester's public_key, mints key + attestation atomically. **CLI** (`scripts/ant-cli-identity-keys.mjs`, wired into `ant-cli-identity.mjs` dispatcher): `ant identity attest-device --new-device LABEL --identity-id ID --attester-key KEY_ID --private-key-file PATH [--new-private-key-file PATH] [--reason TEXT]` drives the full two-leg flow with local keygen; `ant identity revoke-device` / `recover` / `approve-recovery` / `recover-from-paper-key` / `list-keys` are staged scaffolds that print the canonical request shape — Stage B permissions ships their server endpoints. Placeholder `--private-key-file` flag stands in for OS-keychain wiring (Stage B). **Tests** (44 new): `identityKeysStore.test.ts` (15 cases) covers create/mint/revoke/lookup/verifyAttestation positive + negative + recovery happy path; `identityKeysSignature.test.ts` (8 cases) deterministic ed25519 round-trip + sha256Hex + challenge nonce; `attest-challenge/server.test.ts` (6 cases) admin gate + 400/404/409 paths; `attest-device/server.test.ts` (5 cases) including nonce-replay rejection; `ant-cli-identity-keys.test.mjs` (15 cases) covers all 6 sub-verbs flag-parsing + two-leg fetch ordering + new-private-key-file write. Plus `v0.2-regression.test.mjs` skeleton with cases 9a/9b/9c stubbed as `it.todo`. **Out of scope for this PR (Stage B follow-ups)**: modal routing for Tier 2 recovery approvals; OS-keychain bridge to replace the `--private-key-file` placeholder; server endpoints for revoke-device / recover / approve-recovery / recover-from-paper-key / list-keys (CLI scaffolds today). Part of plan ant-substrate-v0.2-2026-05-29 milestones p4-identity-keys-canvas + p4-attest-device-cli + p4-paper-key. |
| Stage A: 403 PermissionDenied payload restructure | ratified PR shape `docs/concepts/` (in-flight) + plan `ant-substrate-v0.2-2026-05-29` milestones `p3-stage-a-403-payload` + `p3-stage-a-grant-cli`, 2026-05-29 | CHANGE | Claude (speedyc swarm spawn) | Permissions UX win: every 403 from `/api/chat-rooms/**` + `/api/grants` now returns a structured `permission_denied` block with `action`, `target_kind`, `target_id`, `target_display_name`, stable `reason` enum (`no_membership` / `room_closed` / `not_room_owner` / `not_org_admin` / `no_grant` / `identity_unresolved` / `tier_required` / `human_consent_required`), `approvers[]` with preferred-flag (room_owner / plan_owner / task_assignee / org_admin / task_creator), an `approve_command` string-equal to the CLI invocation that would grant it, and forward-compatible `approve_url` / `request_id` / `expires_at_ms`. New helper `src/lib/server/permissionDeniedPayload.ts` (builder + reason humaniser + runtime type guard), new approver resolver `src/lib/server/permissionApproverResolver.ts` (room→owner / plan→creator / task→assignee+plan_owner+creator fallback / org→admins / system→empty), new `grants_shim` table + `grantsShimStore.ts` (grantPermission / revokePermission / lookupActiveGrant + listAllGrantsForTests; row shape byte-identical to v0.2 `grants` so the Stage B migration is a row-copy). New `ant grant <handle> <action> --room|--plan|--task|--org|--system <id> [--once|--always-for-room|--always-for-agent] [--revoke]` CLI verb wired into the dispatcher via `ant-cli-grant-verb.mjs` (the standalone `ant-cli-grant.mjs` from the 2026-05-19 caller_grants slice is intentionally untouched — different surface, different endpoint). Backed by new `/api/grants` (POST + DELETE, admin-bearer OR pidChain auth). CLI renderer `ant-cli-permission-denied.mjs` emits 4-6 stderr lines (head + reason + approver + approve_command + optional URL + optional request_id) wired into `sendJson` / `sendJsonWithCookie` in `ant-cli-chat.mjs` AND `throwIfNotOk` in `ant-cli.mjs`. 403 throw sites rewritten: `chatRoomAuthGate.ts:admin_cannot_impersonate_human`, `chat-rooms/[roomId]/messages/+server.ts:human_impersonation_no_terminal` + `rejectMessageIdentity`, `chat-rooms/[roomId]/members/+server.ts:not-in-org`, `chat-rooms/[roomId]/mode/+server.ts:not-a-member`, `chat-rooms/[roomId]/responders/+server.ts:not-a-member`. Legacy `message` strings preserved on every rewrite so existing audit-of-impersonation tests + alerting filters + CLI wedge-hint matches continue to work; new consumers read the `permission_denied` block. 67 new tests across `permissionDeniedPayload.test.ts` (11) + `grantsShimStore.test.ts` (11) + `/api/grants/server.test.ts` (9) + `ant-cli-grant-verb.test.mjs` (13) + `ant-cli-permission-denied.test.mjs` (13) + `chat-rooms/[roomId]/messages/permission-denied-payload.test.ts` (1) + chat-room test sweep continues 548/548 green. Out of scope for Stage A (deferred to Stage B / Part 2): modal routing for approvers, `pending_actions` queue for auto-replay, `permission_requests` audit-trail rows, full owner/admin gate on the grant endpoint (Stage A trusts the CLI surface). |
| `pid_start` ISO 8601 normalisation across writer + reader (PR-A of v0.2 / closes JWPK enterprise-concern #1) | 2026-05-29 4-hour silence forensic across 19 agents — pidChain lookups silently missed when the locale-formatted `ps -o lstart=` string didn't byte-equal the stored value across en_GB ("Fri 29 May 11:11:24 2026") vs en_US ("Thu May 29 11:11:24 2026") vs Windows PowerShell ISO ("2026-05-29T11:11:24.000Z") | CHANGE | Claude (Opus 4.7) | New `src/lib/server/pidStartNormaliser.ts` exports `normalisePidStartToIso8601(raw)` — single canonical helper. Already-ISO input (matched via `/^\d{4}-\d{2}-\d{2}T/` to avoid the "Thu"-substring false-positive) is preserved verbatim so sub-second + timezone offsets from PowerShell `.ToString('o')` survive; locale strings go through `new Date(...).toISOString()`; null/empty/non-string/garbage returns null (never throws). Applied at every write site: `upsertTerminal` (UPDATE-existing + INSERT-new branches share one pre-normalised value), `autoRegisterTerminalForSpawnedSession` (normalises the `ps -o lstart=` output), `adoptExternalProcessForTerminal` (normalises the `pidStart` arg before the ON-CONFLICT upsert), and `parsePidsList` in `/api/identity/register/+server.ts` (normalises client-supplied chain entries so `liveNameConflict` + `pidConflict` checks compare ISO-to-ISO). Applied at every read site: `lookupTerminalByPidChain` (normalises each chain entry's `pid_start` before binding to the SQL parameter — DB stores ISO so we ensure the comparison value is ISO too) and `getLiveTerminalByPid` (same treatment). Applied at the CLI client-side belt-and-braces: `scripts/ant-cli-identity-chain.mjs` `readProcessStartTimePosix` now normalises before returning (so the wire format is canonical even before the server hits it), and `readProcessStartTimeWindows` also runs the helper defensively against future PS format drift. JS-equivalent normaliser inlined in the .mjs (no .ts import path from .mjs); kept identical to the TS canonical with a one-line "See ..." comment. Tests: new `pidStartNormaliser.test.ts` (7 tests — day-month vs month-day → same ISO; Windows already-ISO passthrough; whitespace trim; null/undefined/empty → null; non-string → null; unparseable garbage → null). Extended `terminalsStore.test.ts` (1 new regression test — upsert with en_GB lstart, lookup with en_US lstart, asserts same row matches; pre-existing lookup tests updated to use parseable lstart-shaped strings since arbitrary tokens like `'fixed-start'` now collapse to null at the boundary). Extended `ant-cli-identity-chain.test.mjs` (4 changes — `pidStart` ISO-output assertion, day-month/month-day equivalence assertion, garbage→null assertion, `processIdentityChain` updated to feed parseable lstart strings + assert ISO output). 7+20+12+25+25 = 89 targeted tests green (pidStartNormaliser + terminalsStore + ant-cli-identity-chain + identity/register/server + terminalLifecycle). svelte-check unchanged (4 pre-existing validation-summary errors). component-size unchanged (7 pre-existing oversized .svelte files). LOC: +~150 impl + ~95 test = ~245 LOC total; well under the 400-LOC architectural-rethink threshold. |
| v0.2 regression corpus skeleton with 9 incident-linked test stubs (2026-05-29) | JWPK msg_5l76efhrxg /goal ratifying the v0.2 schema cut-over plan from `docs/concepts/ant-v02-identity-and-recovery.md`; 9 cases banked from 2026-05-29 AM silence forensic + PM @speedyc trip + @codex4 race instances, plus ratified enterprise scenarios (fleet restart msg_rj7xtj7krk, peer-driven upgrade msg_rj7xtj7krk, Stevenson key-loss msg_gtzwsh340p, nifty-orphan-grant msg_mjh7rgi3wa) | CHANGE | Claude (claudev4) | New `scripts/v0.2-regression.test.ts (new) + docs/v0.2-regression-corpus.md (new)` ship the skeleton for the v0.2 cut-over gate. Each `it.todo()` stub names a single failure shape and is paired with a comment block citing the incident date + source room+msg_id + the v0.2 invariant from `docs/concepts/ant-v02-identity-and-recovery.md` that closes it. 9 stubs cover: (1) locale-format pid_start mismatch → `runtimes.pid_start_iso`; (2) shadow-terminal shadowing → `UNIQUE INDEX (agent_id) WHERE status='live'` on runtimes; (3) dual-bind on fresh register → derived-not-cached fanout target; (4) six-rooms × stub-id race → audit_events instead of row UPDATE; (5) competing-rebind race → atomic swap on `agents.current_runtime_id`; (6) fleet-restart auto-reclaim → `ant admin reclaim --all-stale --auto-approve`; (7) peer-driven upgrade reclaim → `reclaim_requests` with `requesting_agent_id != agent_id`; (8) nifty-orphan-grant → `tools` row as single source of truth + `ant audit orphans`; (9) Stevenson multi-key survival → `agent_trust_keys` table allowing N keys per agent. `vite.config.ts` test include glob extended to pick up `scripts/**/*.test.ts` so the corpus is actually discovered by vitest (previously only `scripts/**/*.test.mjs` was included). Bodies stay `todo()` until the v0.2 schema lands — they would fail (correctly) on the current schema otherwise, defeating the gate. Assertions land incrementally with each migration PR per the lifecycle in `docs/v0.2-regression-corpus.md#lifecycle-of-a-row-in-this-table`. Permanent CI gate (every schema-touching PR runs the corpus after v0.2 cut-over) is **proposed, awaiting JWPK ratification** per concept doc §Open Questions for JWPK #1. Verification: `bun test scripts/v0.2-regression.test.ts` shows 9 pending / 0 fail; `bun run code-qa` clean. Capability tracked as the v0.2 cut-over gate — each todo() stub becoming a real assertion is the contract every v0.2 migration PR must honour. |
| fix(identity): auto-rebind room_memberships on register when stale binding exists (PR-B of v0.2 / closes JWPK enterprise-concern #5) | JWPK enterprise-concern #5 — @speedyc dual-bind 2026-05-29 (fresh `ant register --name SpeedyC` created new terminal_record but left existing room_memberships pointing at the abandoned old terminal; UI roster showed @speedyc present, pty-inject fanout silently dropped to dead pane; same shape as 33 stale fanout bindings bulk-fixed earlier in the morning) | CHANGE | Claude (claudev4) | `src/routes/api/identity/register/+server.ts + src/lib/server/roomMembershipsStore.ts` — auto-rebind on register, closes the dual-terminal trap that bit @speedyc in v4.1 on 2026-05-29. After the existing upsertTerminal + handle-morph (Phase B) block returns, the endpoint now sweeps `getLiveTerminalsByHandle(handleValue)` for OTHER live terminals owning the same handle. For each candidate that passes `isCandidateStale(candidate, nowMs)`, a single transaction atomically (1) re-points every `room_memberships` row from old → new `terminal_id` where `revoked_at_ms IS NULL` and handle matches, (2) flips the old terminals row to `status='archived'` (only if currently 'live' so we don't churn already-archived rows), (3) marks the old `terminal_records` row as `superseded_at_ms = nowMs` so picker queries that filter on `superseded_at_ms IS NULL` skip it. Self-rebind (same terminal_id from the upsert path returning an idempotent row) is short-circuited by `if (candidate.id === terminal.id) continue` so the loop never tries to steal memberships from itself on a same-name + same-PID re-register. `isCandidateStale` mirrors the agentStatusPoller heartbeat sweep window — 5 min on `max(last_message_sent_at_ms, last_pty_byte_at_ms)`, plus instant true for `status IN ('archived', 'deleted')` or `pane_status='stale'` or `latest=0` (never-touched row, freshly-spawned daemon stub that the new register is replacing). Safe-by-construction: an old terminal with a recent heartbeat keeps its memberships and stays live — `isCandidateStale` returns false on rows with traffic in the last 5 min, so the rebind never fires on a row that's actively chatting. Structured log emitted on every successful rebind: `[auto-rebind] handle=@X old=<id> new=<id> rooms=<id>,<id>,<id> count=N` per JWPK msg_5xjtox2059 audit rule ("any operational decision invisible to the operator is a bug"). 5 new vitest tests in `src/routes/api/identity/register/server.test.ts` cover: (i) stale 10-min-old binding → rebind happens, memberships moved, old archived, old terminal_records.superseded_at_ms set; (ii) fresh 30s-old binding → rebind skipped, memberships untouched, old terminal still live; (iii) never-touched row (heartbeat null) → rebind happens (latest=0 treated as stale); (iv) no prior binding → endpoint no-op clean 201; (v) self-rebind (same terminal_id from idempotent upsert) → skipped, no archive churn. 30/30 register tests pass (25 pre-existing + 5 new). 103/103 pass across `roomMembershipsStore.test.ts + terminalsStore.test.ts + terminalLifecycle.test.ts + terminalRecordsStore.test.ts`. svelte-check 4 errors unchanged (all pre-existing in validation-summary +server.ts). |
| Terminal lifecycle Phase C3 — picker queries filter on `terminals.status` (0.1.14) | JWPK A Team msg_emnmgs1y9t 2026-05-29 (screenshot showing the invite picker still listed dead handles after 0.1.13 deploy; called out as an explicit miss against the original Phase C2 scope which only wired the participants pane) | CHANGE | Claude (claudev4) | Plugs the gap between the Phase A3 archive flip and the picker UI that JWPK named in his original msg_w7sfmc4hpp ("reflected in the invites pane"). `listLiveTerminalRecords` + `listKnownHandles` in `terminalRecordsStore.ts` now LEFT JOIN `terminals t ON tr.session_id = t.id` and WHERE `(t.status IS NULL OR t.status = 'live')`. The NULL-tolerant predicate preserves rows that lack a matching terminals row entirely — pre-A1 historical data + remote-bridge rows that only exist in terminal_records. Without that fallback, those rows would silently drop out of every picker, which would be a worse regression than the bug being fixed. `listAllPickableHandles` inherits the fix for free since it derives from `listLiveTerminalRecords`. 7 new vitest tests cover: (C3a) live-status row included; (C3b) archived dropped; (C3c) deleted dropped; (C3d) orphan/pre-A1 row preserved; (C3e) listKnownHandles also drops archived; (C3f) listAllPickableHandles also drops archived; (C3g) orphan derived-handle still appears. 40/40 terminalRecordsStore tests pass. svelte-check 4 errors unchanged (all pre-existing in validation-summary). Operational note: the live server snapshot must be rebuilt + `launchctl kickstart -k com.ant.fresh`'d for this to take effect — the scoop bucket pipeline only ships the CLI, not the Mac-side SvelteKit build. That deploy-gap was the underlying cause of the bug being reported; baking the server rebuild into post-merge sequence for any slice that touches server-side. |
| Terminal lifecycle Phase B — handle_aliases on handle change (0.1.13) | JWPK A Team msg_7uvr35x0xr 2026-05-29 Q4 default "new=primary so chat send works under the current name" (Phase B of 0.1.13 lifecycle slice, builds on Phase A1 commit `d487df6`) | CHANGE | Claude (claudev4) | `POST /api/identity/register` now accepts an optional top-level `handle` field and wires it into the Phase A1 alias trail. Flow: after `upsertTerminal` returns a `terminal_id` (which doubles as the `terminal_records.session_id` when a record exists), look up the record via `getTerminalRecord(terminal_id)`. If the record exists AND its stored `handle` is non-empty AND differs from the supplied one, call `appendHandleAlias(terminal_id, existingHandle)` to preserve the OLD handle in `handle_aliases`, then `updateTerminalRecord(terminal_id, { handle: newHandle })` to make the new handle primary (Q4 default — chat send needs to work under the current name). No-ops cleanly when: (a) `handle` field omitted, (b) supplied handle matches existing, (c) no `terminal_records` row exists (register endpoint never creates them — that's POST /api/terminals' job). Phase A1's `appendHandleAlias` already dedupes + normalises leading-@ so repeat morphs stay clean. 5 new vitest tests in `src/routes/api/identity/register/server.test.ts` (first-register-no-aliases / morph @claudev4→@claudev5 / chain @claudev4,@claudev5→@claudev6 / no-op same-handle / no-op omitted-handle). All 21 register tests + 25 Phase A1 lifecycle tests pass. svelte-check 4 errors unchanged (all pre-existing in validation-summary +server.ts). Phase A2 (register conflict checks) + A3 (poller status flip) + C (path endpoint / UI) still pending. CLI side already plumbs handle through `meta.handle`; this slice adds a parallel top-level `handle` field for the simpler write path — the CLI lift to send `handle` at top-level is a follow-up (CLI passes `meta.handle` today, which this endpoint still preserves on the `terminals` row via the `meta` field). |
| Terminal lifecycle Phase A2 — auto-pane + register conflict checks (0.1.13) | JWPK A Team msg_w7sfmc4hpp + msg_7uvr35x0xr 2026-05-29 (Phase A2 of lifecycle slice) | CHANGE | Claude (claudev4) | Builds on Phase A1 (`d487df6`) to deliver the operator-facing half of the lifecycle slice. Two changes land together. (1) `scripts/ant-cli-register.mjs` now auto-detects the caller's pane when `--pane` is unset (JWPK design Q1 default A — caller-friendly default). Resolution order: explicit `--pane` → `runtime.envTmuxPane` (injected by tests) → `process.env.TMUX_PANE` (tmux export) → `process.env.WEZTERM_PANE` (wezterm export) → null. tmux's `TMUX_PANE` (e.g. `%42`) and wezterm's `WEZTERM_PANE` are already populated for every interactive shell so the registration call now lands with a pane id without the user having to look it up. (2) `POST /api/identity/register` now layers two 409 rejections over the existing upsert path (JWPK design Q2 default B — helpful error with the exact recovery command including conflicting terminal id/name). Rule (a) live-name conflict: when the name is already owned by a `status='live'` terminal whose `(pid, pid_start)` differs from the caller's, reject with `"Name '<n>' is already live on terminal <id>. Reclaim with --handle <existing-handle> or pick a different --name."`. Rule (b) PID-in-use: when the caller's leaf `(pid, pid_start)` is already bound to a different live terminal, reject with `"PID <p> (start=<s>) is already bound to live terminal '<name>'. Archive it first or use a different shell."`. Both rules consume the Phase A1 helpers `getLiveTerminalByName` + the new `getLiveTerminalByPid` (added in this slice — handles `pid_start IS NULL` wildcard match for parity with `lookupTerminalByPidChain`, archived/deleted rows excluded so PIDs can be recycled once their prior owner is archived). Same-name + same-`(pid, pid_start)` re-register stays idempotent (Phase A2 case c); archived rows free their name for re-use (Phase A2 case d). Pre-existing M3.2b d test updated to keep the same `(pid, pid_start)` across re-registration (path-B pane-change semantics unchanged; the test was previously exercising a now-forbidden PID swap under a live name). 10 new vitest tests: 6 in `ant-cli-register.test.mjs` (runtime-injected pane, TMUX_PANE env, WEZTERM_PANE env, `--pane` wins precedence, omitted-when-nothing-detected) + 4 in `register/server.test.ts` (cases a-d). 66/66 tests green across `ant-cli-register.test.mjs` (21) + `register/server.test.ts` (20) + `terminalLifecycle.test.ts` (25). svelte-check unchanged (4 pre-existing errors in validation-summary +server.ts only). Phase A3 (agentStatusPoller flipping `live` → `archived` on pane disappearance), Phase B (handle_aliases write path), and Phase C (last_path endpoint + UI surface) layer on top in follow-up PRs. |
| Terminal lifecycle Phase C2 — invites pane archived treatment + Reclaim button (0.1.13) | JWPK A Team msg_7uvr35x0xr 2026-05-29 greenlit (Phase C2 of lifecycle slice, follows Phase A1 commit `d487df6`) | CHANGE | Claude (claudev4) | Wires the terminals.status column from Phase A1 through to the participants pane so the operator can see + recover archived terminals without restarting the slice. Three coordinated changes: (1) `/api/chat-rooms/:roomId/agent-statuses` extended — `StatusRow` projection gains `lifecycle_status: 'live'\|'archived'\|'deleted'\|null` (null when LEFT JOIN finds no terminal bound to the membership); SELECT pulls `t.status AS lifecycle_status`; response payload surfaces it as `lifecycleStatus` per-handle. (2) `ParticipantsPanel.svelte` extended — `StatusEntry` carries the new field; `refreshStatuses` fetch handler folds it into the panel state; archived members render with a muted background + `📦 archived` pill in place of the focus/idle/working dot, plus a `Reclaim` button next to the row that fires a POST to the new endpoint with the caller's pidChain in the body. Reclaim button has a per-handle in-flight guard (`reclaiming: Set<string>`) so rapid double-clicks don't fire concurrent POSTs. (3) New endpoint `POST /api/chat-rooms/:roomId/members/:handle/reclaim`: gates on `requireChatRoomReadAccess`; resolves `(roomId, handle)` → membership → bound terminal; idempotent 200 (`{ ok:true, alreadyLive:true }`) when already live; 409 when deleted (intentional, fresh-register required); on archived, parses `pidChain` from body and if the caller resolves to a different live terminal via `lookupTerminalByPidChain`, re-points membership.terminal_id to the caller's terminal via `addMembership`'s existing upsert AND flips the caller's terminal status to live, otherwise flips the bound terminal status to live in place. 12 new tests: agent-statuses gains 2 (live default + archived) + a null-when-no-binding case; reclaim endpoint covers all 7 acceptance criteria (401 unauth / 404 stranger / 404 stale-FK via PRAGMA-toggled raw insert / idempotent already-live / 409 deleted / archived flip / pidChain re-point with old terminal staying archived); ParticipantsPanel test pins the source-template tokens for the archived/reclaim branches + asserts SSR-without-statuses-data renders no Reclaim button. svelte-check unchanged (4 pre-existing errors in validation-summary `+server.ts` only). MVP scope per JWPK greenlit msg — visual polish (animation, archive-history, undo-toast) deferred to follow-up. Phase A2 (`ant register` rule a+b conflict checks), A3 (agentStatusPoller flips status), B (handle_aliases write), C1 (last_path endpoint) untouched in this slice. |
| Terminal lifecycle Phase A3 — poller flips to archived on pane-gone + heartbeat-stale (0.1.13) | JWPK A Team msg_w7sfmc4hpp + msg_7uvr35x0xr 2026-05-29 (Phase A3 of lifecycle slice) | CHANGE | Claude (claudev4) | Wires the Phase A1 `setTerminalStatus` helper into `agentStatusPoller.ts` so terminals self-archive when they can no longer be reached. Two new pathways: (1) per-terminal pane-gone — `pollOneTerminal` now calls the existing `verifyPaneTargetState` from `pty-inject-bridge.ts` (NOT a new tmux probe) before sampling fingerprint bytes; when it returns `'stale'` (capture-pane exits non-zero ⇒ pane is gone), the row flips to `status='archived'` and the cascade returns early. (2) heartbeat sweep for remote + paneless rows — `runOnce` walks `listAllTerminals().filter(t => t.agent_kind === 'remote' || !t.tmux_target_pane)` after the fingerprint loop and archives any row whose `max(last_message_sent_at_ms, last_pty_byte_at_ms)` is older than `Date.now() - 5*60*1000` (JWPK Q3 default A). Rows that have never been touched (latest=0) are skipped so freshly-spawned bridges aren't killed on the first tick; already-archived/deleted rows are also skipped to avoid churn. 7 new vitest cases in `agentStatusPoller.test.ts` cover: stale→archived early return + captureFn-not-called (Test 1); verified→continues to fingerprint sample (Test 2); idempotent double-tick on stale row stays archived (Test 6); remote heartbeat stale→archived (Test 3); remote heartbeat fresh stays live (Test 4); remote already-archived sweep is a no-op + updated_at unchanged (Test 5); remote with no heartbeat history (latest=0) stays live (new test 7). Shared `beforeEach` stubs `pty-inject-bridge.setSpawnImplForTests` with a verified-capture default so pre-existing tests don't accidentally hit the real tmux during Phase A3's new gate (validated by running the suite with `ANT_TMUX_BIN=/nonexistent/tmux` — still 29/29 green). 54/54 vitest pass on the targeted run (`agentStatusPoller.test.ts` + `terminalLifecycle.test.ts`). svelte-check unchanged (4 pre-existing errors in `validation-summary/+server.ts` only). Phase B (handle_aliases write on register-with-changed-handle) + Phase C (POST /api/terminals/:id/path endpoint + UI surface) remain follow-up PRs. |
| Terminal lifecycle Phase A1 — schema + store helpers (0.1.13) | JWPK A Team msg_w7sfmc4hpp + msg_7uvr35x0xr 2026-05-29 (Phase A1 of lifecycle slice) | CHANGE | Claude (claudev4) | Foundational data layer for the 0.1.13 terminal lifecycle slice. Three new SQLite columns (idempotent ALTER TABLE under the existing applySchemaMigrations runner): `terminals.status` (NOT NULL DEFAULT 'live' CHECK live\|archived\|deleted) as the source of truth for "is this terminal a candidate for room_memberships binding" — distinct from pane_status (tmux verification) and agent_status (activity); `terminals.last_path` (TEXT NULL) for the last shell working directory so recovered shells can `cd $last_path`; `terminal_records.handle_aliases` (TEXT NULL JSON array) preserving prior handles when a shell morphs handle mid-session. Four new exports in `terminalsStore.ts`: `setTerminalStatus` (idempotent flip used by agentStatusPoller + operator commands), `setTerminalLastPath` (set/clear w/ whitespace normalisation matching setTerminalModel), `getLiveTerminalsByHandle` (handle-binding conflict check via room_memberships join, excludes archived/deleted/orphans), `getLiveTerminalByName` (name conflict check, archived rows free the name for re-use). Two new exports in `terminalRecordsStore.ts`: `appendHandleAlias` (NULL → ["@new"] on first append, dedupes, normalises leading @) + `getHandleAliases` (NULL → []). 18 new vitest tests in `terminalLifecycle.test.ts` (mirrors `terminalsStoreModel.test.ts` scaffolding + per-test temp DB to isolate addMembership preamble side-effects). Phase A1 ships data only — Phase A2 (`ant register` rule a+b checks via the new conflict helpers), Phase A3 (agentStatusPoller flips status='live' → 'archived' when pane is gone), Phase B (handle_aliases write on register-with-changed-handle), and Phase C (POST /api/terminals/:id/path endpoint + UI surface for last_path + status) layer on top in follow-up PRs. CLI/pollers/endpoints untouched in this slice. |
| `ant router` SSE consumer CRLF + unhandled-rejection cleanup (PR #86 follow-up) | gemini-code-assist review of PR #86, follow-up cleanup 2026-05-29 | CHANGE | Claude (claudev4) | Two bugs flagged by gemini in its review of PR #86 (merged today as commit `048cd4a`). (1) CRLF handling in `scripts/ant-cli-realtime-sse.mjs`: SSE spec allows CRLF line endings + CRLFCRLF block delimiters, and HTTP proxies between client and server sometimes rewrite. `drainEventStream` now normalises buffer to LF (`buffer.replace(/\r\n/g, '\n')`) before searching for `\n\n` so block detection works under either ending; `parseSseBlock` now splits lines on `/\r?\n/` so a trailing `\r` cannot end up inside the parsed field value (which would have made `id: 42\r\n…` parse as `id='42\r'` and break Last-Event-ID resume). (2) Unhandled rejection in `runStartSse` in `scripts/ant-cli-router.mjs`: the `void injectMessage(...).then(() => …)` chain had no `.catch`, so if `sendTextImpl` threw (e.g. `wezterm` not on PATH) the rejection was unhandled — Node 25+ would crash the process, current Node deprecation-warns. The `.then` now chains a `.catch` that writes the error to `runtime.writeErr` AND resolves `firstMessagePromise` when `runOnce`, so `--once` doesn't hang on a permanently-pending await. 2 new vitest tests added in `scripts/ant-cli-realtime-sse.test.mjs` (CRLF `parseSseBlock` + CRLFCRLF stream-chunk dispatch). Local `vitest` still hung with `ERR_IPC_CHANNEL_CLOSED` per PR #86's ship note, so verification was done via inline node imports: 7/7 CRLF + LF stream-dispatch assertions + 3/3 router-driver no-rejection assertions all pass. svelte-check unchanged (4 pre-existing errors in validation-summary +server.ts only). |
| `ant router` SSE push delivery (0.1.12) | JWPK Tauri room msg_oiu700bmel 2026-05-29 ("Yes!! It needs delivery"), follow-up to xenoCC 0.1.11 smoke green | CHANGE | Claude (claudev4) | New `scripts/ant-cli-realtime-sse.mjs`: exports `parseSseBlock` (spec-compliant — field/value parsing with leading-space strip, multi-line data joining, heartbeat/comment skip, malformed-JSON returns `data:null` rather than throwing) and `startSseSubscriber({ runtime, roomId, onEvent, onError?, onConnected?, sleepImpl? })` (bearer-first auth mirroring `lookupRoomToken` from PR #68 dual-shape walk → `Authorization: Bearer <token>` header when room token present, else pidChain query param fallback; exponential backoff reconnect capped at 30s with `Last-Event-ID` resume; `stop()` aborts in-flight fetch via AbortController). Wired into `ant router start` with new `--mode sse|poll` flag (default `sse`, `ANT_ROUTER_MODE` env override). New `runStartSse` driver shares the existing `routeFreshMessages` → `injectMessage` dispatch path the polling loop uses — only the message-source changes. Legacy polling preserved as fallback. Server-side `requireChatRoomReadAccess` already accepts bearer-on-GET (since PR #68); NO server changes needed for this slice. 8 new vitest tests written; local `vitest` is currently crashing with `ERR_IPC_CHANNEL_CLOSED` due to repo-wide ABI mismatch between vitest worker pool and the installed Node version, but `parseSseBlock` smoke-passed via inline node import (5/5) and the SSE endpoint contract was verified live with `curl -H "Authorization: Bearer ..." /api/realtime/4h4dq9ixa1/events` returning `: connected` + `data: {"type":"connected","latest_seq":0}` as designed. Tests will be exercised in CI on push. |
| `ant invite redeem` persists tokenSecret to ~/.ant/config.json | xenoCC quickpaste msg 8729 (config mtime root-cause) + msg 8733 (router wedge), antV4 / Tauri room 2026-05-28 | CHANGE | Claude (claudev4) | Found by xenoCC: CLI had a config.json READER (PR #68 / 6870a08 dual-shape walk) but never a WRITER — every `ant invite redeem` / `ant invite join-url` succeeded server-side but the locally-minted tokenSecret was discarded, leaving stale tokens in config. Result: routers read May-22 manually-hot-patched tokens → server rejects → pidChain fallback → 401 wedge on Windows where the router subprocess pidChain dies at MSYS2 boundaries. Fix: new `scripts/ant-cli-config-write.mjs` exports `persistRoomTokenToConfig({ roomId, tokenSecret, handle?, serverUrl? })` — atomic write via sibling .tmp + rename, recovers from malformed config by treating it as empty, normalises handles missing leading @, writes BOTH flat shape (PR #68 primary read path) AND byHandle shape (backward-compat with pre-PR-68 readers), preserves unrelated rooms' entries verbatim. Wired into both `runRedeem` + `runJoinUrl` after the successful 200 response BEFORE the auto-register call; failure to persist is a warning-line on stderr only, never changes the redeem exit code. 14 new tests covering fresh-create, stale-overwrite, unrelated-room-preservation, dual-shape write, server_url omission, handle normalisation, missing-handle no-byHandle, input validation, malformed-config recovery, mtime advance + 4 readRoomTokenEntry tests. Existing 23 invite tests pass (3 pre-existing join-url failures unchanged). svelte-check unchanged. |
| `ant invite redeem` pidChain-aware re-bind (Point 2) | JWPK msg_mrhzsoab7s + xenoCC quickpaste 8675 + 8733, antV4 / Tauri room 2026-05-28 | CHANGE | Claude (claudev4) | Completes the Xeno windows-cli-auth-wedge fix: the 2026-05-22 liveness check (aeef816) caught stale-binding cases via `pid_start: null` + expired TTL, but `findTerminalRecordByHandle` could still resolve back to the SAME stale row, leaving the user wedged after a fresh register cycle. `bindRoomHandleToLiveTerminal` now accepts an optional `callerPidChain`; when the existing binding fails liveness and pidChain is supplied, it resolves the caller's actual live terminal via `lookupTerminalByPidChain` BEFORE falling back to the legacy handle→record lookup. Threaded end-to-end: CLI `ant invite redeem` + `ant invite join-url` send `processIdentityChain()` in the request body → `/api/chat-rooms/:roomId/join-with-token` parses pidChain → `bindTokenToRoomMembership` forwards `callerPidChain` → both `existing` and `inviteAgentToRoom` branches re-bind with caller pidChain. Tests: 4 new (caller-pidChain re-bind, fallback when pidChain doesn't resolve, no-op when binding is still live, default empty for legacy callers) on top of the original 4 liveness tests. Old shape (`{ tokenSecret }`-only) still accepted — pre-0.1.10 CLIs and browser callers unaffected. |
| Open-usage daemon integration on /terminals + /agents | JWPK msg_300r0u8dlx + msg_4rbn05cztw + msg_b0rkudqz1g + msg_m1bwd2rcv1 + msg_iot7i1qena + msg_fdrwhloitr antV4 2026-05-28 | CHANGE | Claude (claudev4) | New typed proxy + cache `src/lib/server/openUsageProxy.ts` (30 s in-memory TTL, 1.5 s fetch timeout, soft-fails to cached-or-empty when :6736 unreachable). `GET /api/usage` returns the current UsagePayload; `GET /api/usage/history?limit=N` returns recent snapshots newest-first. `usage_snapshots` table + `usageSnapshotStore` + `usageSnapshotPoller` (12 h cadence, ANT_USAGE_SNAPSHOT_INTERVAL_MS overrideable for tests, boots via hooks.server.ts, captures full UsagePayload as JSON for trend lines). Both `/terminals` and `/agents` mount `UsageStrip` top-of-page (provider pills with session %, plan, today $, 14-point sparkline of session % from history) and an inline `UsageBadge` next to every chip / card-header (provider name + session %, warn/hot styling at ≥80% / ≥95%). agentKind↔providerId join is a permissive case-insensitive substring match so "claude-code" / "codex-cli" / "Copilot" all resolve; `/agents` uses `agent.handle` as the join key (e.g. @claudev4 → claude, @speedycodex → codex). When the daemon is unreachable AND no cache exists, the strip renders a one-line notice that open-usage is required with an inline link to openusage.ai (per msg_m1bwd2rcv1 + msg_fdrwhloitr). Tests: 7 proxy tests (cache TTL, bypass, soft-fail on refused/HTTP-500/malformed, stale-cache fallback) + 5 snapshot-store tests (insert, newest-first ordering, limit, malformed-row skip). |
| /rooms delete cross-room cookie fallback | JWPK msg_athx11bshr antV4 2026-05-28 | CHANGE | Claude (claudev4) | `requireChatRoomMutationAuth` gains Step 3b: when a browser-session cookie is presented that was minted scoped to a DIFFERENT room (Step 3 fails), the gate now resolves the cookie via `resolveBrowserSessionSecretIgnoringRoom` and re-gates on `isHandleMemberOfRoom(targetRoomId, handle)`. Security boundary preserved — the fallback relaxes cookie-scope but still requires a `chat_room_members` row for the caller in the target room. Closes the silent-401 that caused JWPK's "I click delete and nothing seems to happen" report when acting on rooms from `/rooms` whose cookie was minted in a different room. `RoomStrip.svelte` also gains an inline error surface so any future server rejection becomes visible immediately rather than silently disappearing. Tests: AC1 (member → fallback allows) + AC2 (non-member → still 401) in `chatRoomAuthGate.test.ts`. |
| Per-terminal model flag + CLI→model grouping + settings store | JWPK msg_fespxsi2lu + msg_05lh00n3wg antV4 2026-05-28 | CHANGE | Claude (claudev4) | New `terminals.model` nullable column (free-form string, settings owns the canonical list). New `modelKinds` localStorage store mirroring `agentKinds` exactly (enabled[] + add/remove/toggle/reset + init from SimplePageShell). New Models section in `/settings` Tools tab pills/add/reset alongside Agent Kinds. New `setTerminalModel` + `listTerminalModelsByIds` helpers in `terminalsStore`. New `PATCH /api/terminals/:id/model` endpoint (admin-bearer OR browser-session ownership, mirrors `/settings` IDOR fix). `/api/terminals` GET batches model lookup and returns it on each record. `/terminals` page rebuilds grouping as CLI heading → model subheading → chips with inline `<select>` model picker per chip (optimistic update + soft-fail to reload). Unspecified model folds into its own subgroup that renders last. `/agents` model badge deferred — `FleetAgent` has no terminal join; needs a separate slice to compute. 7/7 tests pass; svelte-check clean across 3,641 files. Purely aesthetic per JWPK ("how many Kimis in Codex vs Codex in Codex"). |
| Manual canvas annotation audit log | JWPK strategic-task slice 6 (manual canvas audit purpose), 2026-05-24 | CHANGE | Claude/Claude (claudev4) | New `manual_element_annotations_audit` table appends a row on every create/update/delete via the existing POST/PATCH/DELETE annotation endpoints. New `GET /api/manual/states/:s/:st/annotations/:el/audit` returns history (limit 50 default, max 200). Inspector gains a collapsed Audit section that summarises each entry (`created` / `deleted` / `changed name, position, CLI…`) with author + timestamp. Closes the third of JWPK's three purposes from the original sketch (learning/notes/audit). |
| Design-styles primitive | JWPK dictation #1, 2026-05-23 | KEEP | Kimi | Banked styles (palette/font/asset/spacing/shadow/border) scoped to org/user/public. SQLite table + store + REST API. 4/4 store tests pass. |
| Away-mode tiers | JWPK dictation #8, 2026-05-23 | KEEP | Kimi | Three presence tiers (away-desk/office/phone) + 0-100 intensity dial. SQLite table + store + REST API. 5/5 store tests pass. |
| Stage presenter auth bridge | JWPK 401 blocker, 2026-05-23 | CHANGE | Codex | `requireStagePresenterAuth` accepts deck password from URL query for pause-context + stage-feedback POSTs. Already shipped in ea1b5c0. |
| Room state and away-mode contract | speed-matters governance recovery, 2026-05-23 | CHANGE | Codex | Added `docs/contracts/room-state-away-mode-v1.md` defining brainstorm, heads-down, delivery, away-from-desk, away-from-office, away-from-phone, and hold states. The contract makes away mode lower-noise delivery rather than idle silence, assigns Chair duties, defines build-many pivots, and sets merge/contact rules while the user is away. |
| Away-mode room control mapping | JWPK away-mode correction, 2026-05-24 | CHANGE | Claude/Codex | Away-mode buttons now persist the selected user tier server-side via `away_modes` using browser-session self-auth or admin bearer, while mapping room state separately: active and away-desk use brainstorm, away-office uses heads-down. This makes away-desk observable to agents instead of only changing a local pill. |
| Room description field | JWPK room-card description, 2026-05-24 | CHANGE | Claude/Codex | Rooms now have an optional user/agent-authored `description` distinct from the auto-derived latest-message summary. A room-mutation-gated PATCH endpoint and inline header editor set or clear the value; room cards prefer the description when present and fall back to the latest-message preview when unset. |
| Auth gate latency instrumentation | auth-gate latency investigation, 2026-05-24 | CHANGE | Claude/Codex | Added `ANT_AUTH_GATE_DEBUG=1` tracing around chat-room read auth resolver steps so operators can capture per-resolver timings for slow 401 diagnosis without logging bearer tokens, cookies, or other credentials. The disabled path emits no trace lines. |
| Accounts bearer in-flight dedup | auth-gate latency investigation, 2026-05-24 | CHANGE | Claude/Codex | Accounts bearer identity resolution now shares one in-flight lookup promise for concurrent requests with the same bearer token, while preserving the existing negative-cache and clearing the promise after resolution. This reduces duplicate upstream pressure during browser/API bursts without changing auth semantics. |
| Dashboard starred-room reorder stale-id fix | rooms reorder persistence follow-up, 2026-05-24 | CHANGE | Claude/Codex | Dashboard starred-room drag reorder now moves by visible room id instead of raw bookmark index, pruning stale bookmark ids before persisting. The Starred strip also renders only bookmarked rooms that actually resolve to loaded rooms, so stale slots cannot pull unstarred rooms into the draggable list. |
| Rooms filter description matching | rooms description follow-up, 2026-05-24 | CHANGE | Claude/Codex | The `/rooms` filter input now matches room names or optional room descriptions case-insensitively, while rooms without descriptions retain the previous name-only behaviour. Placeholder and ARIA copy now say "name or description". |
| Room description empty-state polish | rooms description follow-up, 2026-05-24 | CHANGE | Claude/Codex | Undescribed rooms now show a quiet "+ Add description" text action instead of an italic "No description yet." placeholder plus pencil icon, keeping the editor discoverable without adding repeated visual noise to every room header. |
| Room description at creation | rooms description follow-up, 2026-05-24 | CHANGE | Claude/Codex | `POST /api/chat-rooms` and `createChatRoom()` now accept an optional `description` so a new room lands populated without a separate `PATCH /description` round-trip. Same trim + cap + null-on-empty validation as the PATCH path; the create form gains an optional textarea + char count below the name input. |
| Rooms filter "/" focus shortcut | rooms filter ergonomic, 2026-05-24 | CHANGE | Claude/Codex | The `/rooms` page now treats a plain `/` keypress as "focus the filter input" (Slack/Linear/GitHub pattern). Shortcut is suppressed when the user is already typing into INPUT/TEXTAREA/SELECT/contentEditable or holding a modifier key, so it doesn't hijack normal typing. Placeholder and ARIA copy disclose the affordance. |
| /plans overall progress donut | JWPK msg_iuspae79e0 2026-05-24 | CHANGE | Claude/Claude (claudev4) | `/plans` Active view now shows a non-clickable header donut summarising total completion across all plans + an Unfiled tasks slot. Same SVG geometry as PlanDonutCard with defensive math (0/0 renders 0% not NaN). Hidden on archived/deleted views and on empty workspaces. Pure visual; no interactions, no link, no delete affordance. |
| Browser-session touch debounce | auth-gate write pressure follow-up, 2026-05-24 | CHANGE | Claude/Codex | `touchBrowserSessionLastSeen` now skips the 2-statement write transaction (UPDATE browser_sessions + UPDATE terminals) when the session was touched within the last 30 seconds. Safe under the 30-day session TTL — `last_seen_at_ms` and `expires_at_ms` lag the most recent activity by at most the debounce window. Revocations clear the in-memory cache for affected session ids so a re-issued session under the same id cannot pick up a stale "still fresh" verdict. |
| /asks text filter | rooms-filter follow-up, 2026-05-24 | CHANGE | Claude/Codex | `/asks` page gains a text filter input above the queue. Matches `ask.title` and the resolved room name case-insensitively, applies to both Open and Recently-answered sections, shows a live count when filtering, and offers a Clear button when the filter eliminates all results. `/` keyboard shortcut focuses the input (same defensive shape as the /rooms filter shortcut). Per-session state — no localStorage because the queue churns fast. |
| Manual canvas element-annotation audit log | manual canvas slice 6, 2026-05-24 | CHANGE | Claude (claudev4) | New `manual_element_annotations_audit` table (append-only, indexed on screen/state/element + timestamp) records create/update/delete events per annotation with before/after JSON snapshots. New `GET /api/manual/states/:screenId/:stateSlug/annotations/:elementSlug/audit` returns newest-first. Inspector Audit section collapsed by default with edit-count hint; action-coloured one-line summaries. |
| /plans text filter | plans-filter follow-up, 2026-05-25 | CHANGE | Claude/Kimi | `/plans` index gains a text filter input mirroring the `/rooms` + `/asks` shape. Matches plan title case-insensitively, `/` keyboard shortcut focuses the input, count badge shows N-of-M when filtering, Clear button + filter-aware empty state. Per-session state. |
| Premium Bring-in-App web v0 client | JWPK msg_a0s51ioct6 2026-05-25 (spec step 3) | CHANGE | Claude | Web client surface for the Bring-in-App contract. New `BringInAppRow.svelte` renders pill row below the room header on `/rooms/[id]`; gated on the `bring_in_app_ux` feature flag pulled from `/api/capabilities` in the room loader (soft-fails to locked state). Claude Desktop is the only v0-active adapter; others render disabled with v0.5 tooltip. Launch strategy is URL-scheme `claude://new?text=<encoded>` via hidden iframe + parallel clipboard fallback. `src/lib/bringInApp/adapters.ts` exposes a typed `ClientAdapter[]` so v0.5 can add ChatGPT / Codex / Claude Mobile / Gemini without changing call sites. No server-contract changes from row 154. |
| Premium Bring-in-App server contract | JWPK msg_a0s51ioct6 2026-05-25 (Q2: Yes proceed) | CHANGE | Claude | New `POST /api/chat-rooms/:roomId/bring-in-app` mints a `RoomContextPayload` (last 30 messages markdown + room name/description + open asks markdown) and records the launch in `bring_in_app_launches` (append-only audit table indexed on operator+room). Accepts target ∈ {claude-desktop\|claude-mobile\|chatgpt\|codex-desktop\|gemini}. Auth uses existing `requireChatRoomMutationAuth` (admin bearer / accounts-bearer / browser-session cookie). New feature flags: `bring_in_app_api: true` (always on, OSS self-hosters can wire), `bring_in_app_ux: tier !== 'oss'` (premium-gated). Client-side launch protocols (URL scheme / clipboard / Share Sheet) are NOT in this contract — clients receive payload + decide. Spec at `docs/research/bring-in-app-spec-2026-05-25.md`. |
| Click-to-explain v0 | JWPK msg_kqczdmydxc 2026-05-22 | CHANGE | Kimi | Global  toggle enters explain mode: interactive elements get dotted outlines + help cursor, clicking reveals what/why/docs popover. OSS static explain map with 25 seed entries covering rooms, plans, asks, decks, agents, settings, search, discover, diagnostics, vault, memory, chair, ledger. Shift+? keyboard shortcut toggles mode. Wrapped on /rooms /plans /asks /decks /rooms/[id] /agents /search /discover /diagnostics /vault /memory /chair /settings surfaces. Premium deferred to dynamic map from room memory. |
| Room-card plan progress badge | dashboard plan visibility follow-up, 2026-05-25 | CHANGE | Claude/Kimi | New `GET /api/chat-rooms/plan-progress` endpoint returns per-room aggregated plan completion (sum-completed / sum-total / pct across attached plans). `RoomCard` gains optional `planProgress` field; `RoomStrip` renders a small `RoomPlanProgressBadge` when set. Loader on `/rooms` merges the progress feed into the card list. |
| Context-break enforcement setting | speed-matters β2, 2026-05-23 | CHANGE | Codex | Added per-room `context_break_enforcement` with default `hard`, a read/mutation-gated `/api/chat-rooms/:roomId/context-break-settings` endpoint, and server-side `/messages` enforcement so `include_pre_break=true` cannot bypass a hard context break. |
| Validation schema scope filter | validation-feature-v1 lens scope, 2026-05-23 | CHANGE | Codex | `validation_schemas` now carry `scope=public|user|org` plus `scope_id`; `/api/validation-schemas` returns public rows unauthenticated, matching user/org rows for accounts bearer callers, and all rows for admin-bearer callers. |
| Attachment upload browser-session auth | upload regression, 2026-05-23 | CHANGE | Codex | Composer drag/paste uploads and the side-panel Share a file button now force a browser-session mint before POSTing to the mutation-gated room attachments endpoint, reusing the same identity path as message send. |
| Attachment upload 40 MB cap | upload regression follow-up, 2026-05-23 | CHANGE | Codex | Composer and room attachment storage now allow uploads up to 40 MiB, with client-side preflight and server-side decoded byte-size enforcement sharing the same user-facing 40 MB limit. |
| Room memory bridge | native-bridge memory visibility, 2026-05-23 | CHANGE | Codex | `/api/rooms/:roomId/memories` now returns both file-backed room memories and room-scoped key/value memories written by `ant memory put --scope room --target ROOM_ID`, so room memory panels can show banked context regardless of backing store. |
| Memory-pack CLI bootstrap | memory-pack bootstrap, 2026-05-28 | CHANGE | Codex | `ant memory vault set --path PATH`, `ant memory recall [--MEM-LOCATION PATH]`, `ant memory add [--MEM-LOCATION PATH] (--roomID ROOM_ID \\| --all-rooms) --memID MEM_ID`, and `ant memory remove [--MEM-LOCATION PATH] --roomID ROOM_ID --memID MEM_ID` operate on Markdown memory packs directly. The root is pulled from `ANT_MEMORY_VAULT_PATH`, `~/.ant/memory-vault.json`, or explicit override, so agents do not need a path round trip. Rooms attach memIDs via `linked_rooms` without duplicating memory body text. Agent-join system notice points at the configured memory pack and `ant memory recall --search`. |
| Memory-pack room surface bridge | memory-pack bootstrap follow-up, 2026-05-28 | CHANGE | Codex | `GET /api/rooms/:roomId/memories` now also reads the configured memory-pack root and returns Markdown files whose `linked_rooms` include the room. Memory-pack entries are marked `source: "memory-pack"` and include an `/memory?q=<memID>` href so room memory panels can show README/core memories as linked room-visible references rather than CLI-only recall results. |
| ANT-level default memory policy | JWPK msg_ixeaf5oyt5 + msg_6kzj3ehs0v, 2026-05-28 | CHANGE | Codex | Universal memory-pack entries are now resolved at read time by ANT rather than attached room-by-room by agents. `GET /api/rooms/:roomId/memories` includes configured memory-pack files with `default_room_policy: universal`, `scope: universal`, or `type: core`, while `linked_rooms` remains the explicit room-specific layer. This means new rooms inherit README/core memories by policy without mutating every memory file. |
| ANT Stage artefact kind | JWPK msg_6kzj3ehs0v, 2026-05-28 | CHANGE | Codex | Room artefacts now support `kind: "stage"` as distinct from normal `deck` and `html`. `/api/chat-rooms/:roomId/artefacts`, `chat_room_artefacts` schema/migration, the room artefact panel, CLI validation, and CLI manifest all accept/show `stage`, so `/decks/:deckId` Stage presentations can carry Stage-specific UX instead of masquerading as built deck artefacts. |
| Message reaction summaries | native-bridge reaction chips, 2026-05-23 | CHANGE | Codex | `GET /api/chat-rooms/:roomId/messages` now attaches optional `reactions` summaries to messages with existing reactions: per emoji `count` plus up to five `topReactors`. This lights up Native's optional reaction chip render layer without adding notification fanout. |
| Fleet context-fill visibility | native-bridge agent context chips, 2026-05-24 | CHANGE | Codex | `GET /api/agents/availability` now includes a per-agent `contextFill` value when a linked terminal has a fresh `agent_context_fill` probe reading. The read model applies the same 5-minute freshness policy as the room-local agent-status feed, returning `null` for stale or missing values so Native can show context-window chips without rendering stuck percentages. |
| Stage Safari voice access | stage mobile voice auth, 2026-05-24 | CHANGE | Codex | `/api/voice/elevenlabs` now accepts a valid `deck_id` plus `deck_password` as an alternative to a browser-session cookie. Password-protected Stage viewers on mobile Safari can generate narration through the same deck share credential they used to open the deck, while wrong passwords still fail before any ElevenLabs call. |
| Stage in-deck alternatives control | stage feedback alternatives UX, 2026-05-24 | CHANGE | Codex | Added `GET /api/decks/:deckId/alternatives` and a deck-viewer "See alternatives" control next to slide navigation. Stage now surfaces proposal-track artefacts on the feedback slide and generated downstream slide rewrites in-place, so alternatives created from live feedback are visible during presentation instead of hidden behind chat artefact links. |
| Stage focus password presenter auth | stage mobile focus auth, 2026-05-24 | CHANGE | Codex | `POST /api/decks/:deckId/stage-focus` now accepts the same deck share password as other Stage presenter writes, and the deck viewer forwards the password on focus publishes. This removes background 401s for mobile/tailnet Stage viewers who opened the deck by password rather than a browser-session cookie. |
| CLI-agent prompt channel | dogfood load-bearing finding #6, 2026-05-24 | CHANGE | Claude/Claude (claudev4) | New POST /api/cli-agents/:handleId/prompt body `{text}` + per-agent textarea+Send on /cli-agents. For codex handles the endpoint lazily calls `thread/start` (first prompt) then `turn/start` with `input:[{type:'text',text}]` against the live JSON-RPC bridge, so operators can deliver a brief without knowing codex's app-server method names. Closes the spec-vs-impl drift where bring-in-LLM contract specced an input channel but only the spawn endpoint shipped — without this, /cli-agents Start codex spawned a runtime that an operator could only stop, never instruct. |
| In-room CLI-agent bring-in | dogfood findings #4+#5 follow-up, 2026-05-24 | CHANGE | Claude/Claude (claudev4) | Added `POST/GET /api/chat-rooms/:roomId/cli-agents` plus optional `roomId` on the existing `POST /api/cli-agents` body so spawned codex/pi handles carry a room association. New `listCliAgentsForRoom(roomId)` filters the registry without a new table. Room page now mounts a `RoomCliAgentsPanel` with Bring-in-codex button + per-agent prompt textarea (reuses the prompt channel shipped in PR #52). Closes the dogfood gap where the bring-in affordance lived only on /cli-agents detached from any room. Codex wire-back into chat is NOT in this slice — agents post to cli_hook_events, operator reads via /cli-hooks/:sessionId. |
| CLI ergonomics — dogfood findings #1+#2+#3 | dogfood follow-up, 2026-05-25 | CHANGE | Claude/Claude (claudev4) | New `ant agents bring-in --room ROOM_ID [--cli codex\|pi] [--cwd PATH]` sub-verb wraps the room-scoped endpoint shipped in PR #53 (finding #1: no CLI verb for spawn). `ant rooms create` now accepts both positional (`ant rooms create "X"`) and the canonical flag form (`ant rooms create --name X`) — flag wins when both present — and its missing-name error message includes inline usage rather than relying on the top-level help dump (finding #2). `ant rooms create` success output now includes a Next-steps block (room URL, bring-in command, invite command) so the operator following the JWPK "open a room → add a terminal → bring in a codex" pitch doesn't have to grep `ant --help` for step 2 (finding #3). Top-level `printUsage` now lists `agents` as a verb category. Six new tests in ant-cli-agents.test.mjs + four in ant-cli.test.mjs. |
| pi sendPrompt parity | prompt-channel symmetry, 2026-05-25 | CHANGE | Claude/Claude (claudev4) | pi handles now implement `sendPrompt(text)` by calling `bridge.sendCommand({type:'prompt', message:text})` — the canonical PiRpcCommand verified in `piLifecycle.ts:47`. `/api/cli-agents/:handleId/prompt` (shipped PR #52) now works for pi as well as codex. UI gates in `/cli-agents` and `RoomCliAgentsPanel.svelte` widened from `cli === 'codex'` to `codex \|\| pi`. Room-page panel grows a second "Bring in pi" button (pi-magenta) alongside the codex one. Three new tests (PP1-PP3) in `cliAgentRegistry.pi-prompt.test.ts` mock the pi spawn to verify the glue forms the right command shape without spawning a real pi process. |
| Click-to-explain seeds for CLI-agent bring-in | docs follow-up to PRs #52/53/55, 2026-05-25 | CHANGE | Claude/Claude (claudev4) | Five new EXPLAIN_MAP entries (`agents-bring-in-panel`, `agents-bring-in-codex`, `agents-bring-in-pi`, `agent-prompt-textarea`, `agent-session-link`) cover the operator-facing affordances shipped across the dogfood close-out PRs. RoomCliAgentsPanel.svelte now wraps the corresponding UI elements with Explainable so Shift+? mode reveals the what+why for each. Closes the docs gap where an operator hovering over "Bring in codex" had no inline context for what codex was vs pi, what the prompt channel does, or where output goes. Extends the static OSS explain map from 11 to 16 entries. |
| Room page debug chrome rollback | JWPK msg_h39723t2qx, 2026-05-25 | CHANGE | Codex | Removed the visible global `?` explain-mode pill from the sticky header while preserving the Shift+? keyboard toggle, and stopped rendering `RoomCliAgentsPanel` directly above the chat transcript. The CLI-agent bring-in capability remains in the codebase/API, but the room-page placement is deferred until it can live in a proper menu/rail surface instead of dominating the primary chat view. |
| Stage validation v2 per-claim overlay | JWPK stage validation follow-up, 2026-05-25 | CHANGE | Kimi | New `ClaimValidationOverlay.svelte` renders a modal overlay when a presenter clicks a numbered claim in Stage deck viewer. Overlay fetches `GET /api/validation-runs/by-claim?claimAnchor=...` (new endpoint using `resolveCallerHandleAnyRoom` auth) and displays run history with status badges (passed/failed/pending/running/waived), score, run-by, and timestamp. Claim rows in deck viewer become clickable buttons. Heuristic claim extraction from slide bullets preserved from v1; overlay adds live run lookup without requiring a taskId. |
| Bring-in-App web v0.5 ChatGPT adapter | JWPK msg_a0s51ioct6 2026-05-25 Q2 follow-up, 2026-05-25 | CHANGE | Kimi | ChatGPT clipboard-only adapter added alongside existing Claude Desktop URL-scheme adapter. Shared `buildExternalLLMPrompt(payload, opts)` extracted from Claude-specific builder so all adapters reuse the same room-context shape with per-target trailing sign-off. ChatGPT uses `Could you help me think through this?` as its default trailing. Unavailable adapters bumped to v1. Tests cover shared builder, custom trailing, adapter table shape, availability flags. Ledger row in same commit per discipline. |
| pidChain auth for room-less list endpoint | first-day-user audit follow-up, 2026-05-25 | CHANGE | Claude | `tryPidChainQuery` in `chatRoomReadGate.ts` previously short-circuited with `if (!roomId) return null`, so `GET /api/chat-rooms` (the list endpoint, which has no roomId) had no path through the pid-chain resolver. The CLI doesn't pass a bearer, so `ant rooms list` 401'd while `ant chat send` worked (chat send hits a room-scoped endpoint). Fix: when `roomId` is undefined, look up the calling terminal via `lookupTerminalByPidChain`, derive its primary handle, and return access keyed on that handle so the upper layer filters by membership via `canReadChatRoom`. Room-scoped path unchanged. Bug surfaced in `docs/antchat-first-day-audit-2026-05-25.md` finding §"`ant rooms list` returns 401". |
| CLI room-list pidChain sender | first-day-user audit follow-up, 2026-05-25 | CHANGE | Codex | `ant rooms list`, `ant list chatrooms`, and shared room-name resolution now append `pidChain` to `GET /api/chat-rooms`. The server-side room-less pidChain resolver shipped in PR #59 could not help while the CLI still called the endpoint with no bearer and no pidChain. This closes the sender half of the same first-day-user 401: local registered terminals can list only rooms they can read, with membership filtering still enforced by `canReadChatRoom` server-side. |
| CLI room-scoped read pidChain sender audit | sender/receiver auth-shape audit, 2026-05-25 | CHANGE | Codex | Follow-up audit found the same sender/receiver split on `ant chat pending` and room-linked task verbs. `ant chat pending` now sends pidChain on `GET /api/chat-rooms/messages/pending`; `ant task list --room` sends pidChain on `GET /api/tasks?room=...`; `ant task create --room` sends pidChain in the POST body. No-room task list/create remains admin-only per the fail-closed tasks containment matrix. |
| Task detail auth containment | task security follow-up, 2026-05-25 | CHANGE | Codex | `/api/tasks/:taskId` now applies the same fail-closed matrix as `/api/tasks`: room-linked tasks require room read access for GET and room mutation auth for PATCH/DELETE; standalone or orphaned tasks require admin-bearer. CLI `ant task done` and `ant task assign` now include pidChain in PATCH bodies so room-linked task updates can pass the mutation gate without weakening standalone task containment. |
| Click-to-explain seed expansion | JWPK msg_kqczdmydxc 2026-05-22 follow-up, 2026-05-25 | CHANGE | Kimi | Expanded static explain map from 33 to 42 entries, adding task-oriented seeds (tasks-section, tasks-create, tasks-assign), insights seeds (insights-dashboard, insights-status-breakdown, insights-blocked-list), and artefact seeds (artefact-view, artefact-validate, artefact-frame). Wrapped corresponding UI elements on /plans/insights, /artefacts/[artefactId], and /rooms/[roomId] (Tasks discipline link) with Explainable components. svelte-check 0/0, npm run build green. Ledger row in same commit per discipline. |

| CLI `ant rooms members` + `ant rooms break` pidChain sender | follow-up sweep on sender/receiver discipline, 2026-05-25 | CHANGE | Claude | Two sender gaps from the same audit pattern: (1) `ant rooms members <roomId>` was hitting `GET /api/chat-rooms/:roomId` without a pidChain query — wrapped with `pathWithPidChain` so non-admin callers pass the room-read gate; (2) `ant rooms break <roomId>` was POSTing to `/api/chat-rooms/:roomId/breaks` without pidChain in the body — `requireChatRoomMutationAuth` parses pidChain from the body, so the call now includes `pidChain: processIdentityChain()` in every break-post (with or without reason). Tests updated to assert pathname + pidChain query for the members GET, and pidChain array in the body for the break POST. Per the dual-side auth discipline; found by extending @speedycodex's sender/receiver audit to room-detail-read + break-post after their tasks pass closed. |
| Cross-room visibility primitive | JWPK sweep priority #3, 2026-05-25 | CHANGE | Kimi | `GET /api/agents/visibility` — returns per-room aggregates (agentCount, humanCount, openAskCount, lastActivityAtMs) across all rooms the caller can read, plus fleet-level agent counts (totalAgents, activeAgents, idleAgents) sourced from `terminals.agent_status` / `terminals.agent_status_at_ms`. Room-less auth via `resolveChatRoomReadAccess`. Tests seeded with terminals to prevent silent zero-out regression. |
| Stage deck screenshot/image rendering | JWPK Stage presentation ask, 2026-05-26 | CHANGE | Codex | Markdown renderer now permits sanitized `img` tags so Stage decks can show screenshots and product visuals directly. Deck viewer constrains slide images with max-width/max-height and bordered presentation styling. Tests cover markdown image rendering plus unsafe protocol stripping. |
| Univer JSON artefact read viewer | JWPK urgent shared-deck wiring, 2026-05-26 | CHANGE | Codex | Deck/doc artefact GET endpoints now render `content_format=univer-json` as safe HTML instead of returning 501, so room-owned Univer JSON artefacts open through the existing `/artefacts/:id` iframe path. This is the immediate read/view slice; live collaborative editing remains a follow-up. |
| Univer client viewer on artefact page | JWPK urgent shared-deck wiring, 2026-05-26 | CHANGE | Claude/Codex | `/artefacts/:id` now mounts a browser-only Univer canvas for deck/doc/sheet artefacts with `univer-json` content or no content row yet. The viewer dynamic-imports only the needed Univer plugin, remounts on artefact id changes, listens for Univer command execution, and persists deck/doc snapshots back through the existing PUT artefact content endpoints. Spreadsheet persistence remains preview-only until a spreadsheet content endpoint exists. |
| Seeded Univer demo no-login path | JWPK msg_w62h4zdl9z, 2026-05-26 | CHANGE | Codex | The generated `univer_demo_*` artefact route bypasses the global demo-login page gate so a clean browser can open the urgent Univer demo directly on the tailnet dev server. The matching `univer_demo_content_*` deck PUT path can autosave `univer-json` for that seeded demo artefact without room auth; ordinary artefacts and ordinary deck autosaves remain behind the existing room mutation gate. Tests cover both the bypass and the still-gated ordinary path. |
| Univer JSON text edit + validation lens | JWPK msg_ygr92bayjv, 2026-05-26 | CHANGE | Codex | Univer deck/doc artefacts now keep the validation panel live by extracting text elements from the stored `univer-json` snapshot and feeding them through the existing claim scorer. `UniverViewer` also exposes an ANT-side "Text quick edit" control that edits a selected text element, persists the updated snapshot through the existing deck/doc PUT endpoint, and remounts the canvas so the demo has a reliable edit path while native in-canvas text editing matures. |
| Deck substrate restore — Animotion primary, Open-Slide compatible | JWPK msg_w7mkgg86f7 + msg_7gtmgj9uxs + msg_d3mwrfiaj8 + msg_5ytci3jc3z + msg_ul9cnljkjc + msg_o2979c15hu, 2026-05-27 | CHANGE | Codex | Decks now have an explicit split: normal deck artefacts point at `/d/<slug>` via `ant artefact add --kind deck`; ANT Stage presentations use `ant decks add` and render through `/decks/:deckId` with presenter/talking/live-feedback/alternative-generation/validation behaviour. Stage presentations can be created with `animotionSlug` (primary) or `openSlideSlug` (compatibility), stored as room-scoped rows with `theme=animotion:<slug>` / `theme=open-slide:<slug>`, and rendered inside the Stage shell via the built-deck proxy. Built deck folders are operator-configured outside this repo via `ANT_BUILT_DECKS_ROOTS`, defaulting to `~/CascadeProjects/ANT-Decks`; Dropbox, iCloud Drive, mounted shares, and other filing-system roots are valid, including JWPK's intended `/Users/jamesking/New Model Dropbox/James King/ANTdecks` root. Generated deck output is intentionally gitignored and must not be committed to the server repo. Empty deck artefacts no longer default to Univer; Univer remains explicit for stored `content_format=univer-json` artefacts. `@animotion/core` is now the OSS deck dependency because it is Svelte-native; Open-Slide remains supported for existing built decks. |
| Normal deck build CLI | JWPK msg_rsl5bdeiay + msg_etdzyzf6sx, 2026-05-27 | CHANGE | Codex/Claude | Added singular `ant deck build SLUG` and `ant deck list` for normal deck artefacts. The build command discovers deck folders under `ANT_BUILT_DECKS_ROOTS`, runs `npm run build` inside `<root>/<slug>`, verifies `<root>/<slug>/dist/index.html`, and prints the matching `ant artefact add --kind deck --ref-url /d/SLUG` command. It deliberately does not install dependencies or create Stage presentation rows; package-manager setup remains a one-time deck-folder concern, while `ant decks add` stays reserved for ANT Stage presentations. |

| CLI room-scoped GET pidChain — audit/discussion/status | sender/receiver audit extension #2, 2026-05-25 | CHANGE | Claude | Three more room-scoped CLI GETs lacked pidChain in the query — `ant audit permissions --room`, `ant discussion list --room`, `ant status show --room` — all hitting `/api/chat-rooms/:roomId/audit|discussions|status`. The `hooks.server.ts gateChatRoomReadApi` applies `requireChatRoomReadAccess` to every GET under `/api/chat-rooms/:roomId/*`, so any sender that doesn't thread pidChain 401s. Each script now appends `pidChain: JSON.stringify(processIdentityChain())` via URLSearchParams. `/api/discussions/:id` is NOT under the gate prefix and was correctly left alone. Tests updated to assert pathname + pidChain query separately. 33/33 pass. Per the dual-side auth discipline; this slice extends @speedycodex's audit (7e06dfc / 24fba92 / 23aa7d3) to the remaining room-scoped GET surfaces. |
| Plan lifecycle chooser on /plans active view | JWPK msg_zcsmyfk7e2 ask, 2026-05-26 | CHANGE | Claude/Claude (claudev4) | New `showLifecycleActions` prop on `PlanDonutCard` exposes a per-card delete-or-archive chooser on the /plans default view. Idle state shows a quiet "Delete…" button; click reveals an inline strip with three explicit choices — Archive (PATCH action=archive, recoverable via /plans?show=archived) · Permanently delete (PATCH action=hard-delete, irreversible) · Cancel. Wraps existing server endpoint (coordinator 1ab15e0) — no API change. Archived/deleted views still use the existing `showHardDelete` arm→commit shape since the archive option is moot once already archived. |

| `/rooms` perf — plan-progress 404 + card-link drag conflict | JWPK msg_5umkyxrxr4 2026-05-26 | CHANGE | Claude | Two distinct user-reported issues fixed in one slice. (1) `/api/chat-rooms/plan-progress` was returning 404 + taking ~1.5s because `hooks.server.ts gateChatRoomReadApi` treated `plan-progress` as a missing `:roomId` value and threw 404. Added it to the skip list alongside `messages` and `recovery`. The endpoint itself was always wired correctly — the hook was intercepting before it could run. /rooms page loader fanned out this 404 in parallel with the rooms list, so the 1.5s dominated perceived load time. (2) `RoomStrip` card-body `<a>` was wrapped in a parent `<div draggable={isReorderable}>` for reorder support, which made the browser route ⌘-click / middle-click into the drag-start gesture instead of the link's open-in-new-tab. Added `draggable="false"` on the inner `<a>` — preserves parent reorder UX while restoring normal link gestures. 3 existing tests pass; svelte-check 0/0/0. |
| Stable server transcript-tail throttle | JWPK msg_jfd3x1wlat / msg_f697m69dem 2026-05-26 | CHANGE | Codex | Production profiling found the stable server's main thread spending sampled time inside synchronous child-process spawns from the transcript-tail watcher. The generic watcher no longer scans every terminal record every 2 seconds; it polls a bounded round-robin batch with an in-flight guard and env-tunable `ANT_TRANSCRIPT_TAIL_POLL_MS` / `ANT_TRANSCRIPT_TAIL_MAX_RECORDS_PER_TICK`. Explicit `tailAllOnce()` remains exhaustive for manual catch-up. This keeps transcript ingestion alive without letting background tmux probes block page loads, health checks, or message sends. |
| Plan hard-delete arm step + chooser hover states | gemini-code-assist follow-up on PR #66, 2026-05-26 | CHANGE | Claude/Claude (claudev4) | After PR #66 merged (b8a3838), gemini-code-assist flagged the immediate-commit hard-delete in the chooser as a data-loss sharp edge. Added an `armed-hard` state with the same 5s arm→commit pattern used by the existing `showHardDelete` affordance on archived views. Archive stays one-click (recoverable). Plus hover states on `.lifecycle-hard` and `.lifecycle-cancel` for visual consistency with the rest of the chooser bar. `.lifecycle-hard` now defaults to outlined-warn (transparent fill, warn-coloured text/border) and only FILLS to solid-warn with the pulse animation on the armed-hard state — destructive commit reads visually distinct from the chooser entry. Splits cleanly from #66 because the merge landed before this follow-up. |
| Room-token lookup — legacy byHandle nested shape | xenoCC bug report msg_pi45j28sud, 2026-05-26 | CHANGE | Claude/Claude (claudev4) | `lookupRoomToken` in scripts/ant-cli-browser-session.mjs only read the flat `tokens[roomId].token` shape. Older configs (pre-0.1.7 invite exchange) nest the token under `byHandle["@handle"].token`. The flat-only read returned null for those configs, the bearer-on-GET path was skipped, and the router fell back to pidChain-in-URL → URL-length 502 from the gateway (the exact bug 0.1.9 fixed for the bearer path). Now reads flat first, falls back to nested `byHandle` preferring `default_handle` then any handle. No migration ship in this slice — handler is defensive across both shapes so existing configs keep working without user action. |
| Reply-badge-for-recipient on message rows | NMT feedback #2a (@james), 2026-05-26 | CHANGE | Claude/Claude (claudev4) | Recipient-side discoverability: when current viewer is the parent message author (someone replied to my message) OR is bare @-mentioned in this message body, the row renders a visible badge — "↳ Reply to you" (accent-tinted) or "@ Mentioned you" (ok-tinted). Closes @james NMT feedback msg_3fipvqu8m9: "replies are posted and they just look like normal messages and can get lost by the person looking for them." Pure derivation in `src/lib/chat/addressedToViewer.ts` with 10 unit tests covering the 5-case truth table + edge cases (page placeholder @you, own message, bracketed informational mention, case-sensitivity, third-party parent). MessageRow.svelte adds the badge + accent border-left when addressedToViewer ≠ null. No schema change — uses existing parentMessage prop + listBareMentionHandles body scan. |
| Jump-to-bottom shows new-message count | NMT feedback #B (@mark), 2026-05-26 | CHANGE | Claude/Claude (claudev4) | The jump-to-bottom button on MessageList already existed ("↓ Latest") and sticky-scroll (#A) is already wired via `shouldFollowBottom` + `isNearBottom`. This slice adds the count of messages that arrived while the viewer was scrolled up — Zoom-style "↓ N new" pill when count > 0, idle "↓ Latest" when count = 0 but still scrolled up. Count derivation extracted to a pure helper (`src/lib/chat/newMessagesBelowCount.ts`) with 8 unit tests covering edge cases (empty list, snapshot id paged out, snapshot is newest, shouldFollowBottom override). Tracking: snapshot `lastSeenMessageIdAtBottom` whenever shouldFollowBottom is true; count derives from messages after that snapshot; returning to bottom re-captures the snapshot which naturally resets count. `has-new` class adds stronger pill + pulse animation; respects prefers-reduced-motion. |
| Ordered responder picker + room settings UI | JWPK msg_eshm5ekuh8 + msg_le6o84ipu1, 2026-05-26 | CHANGE | Kimi | Room settings sidebar shows ordered responder list with up/down reorder, add/remove. Heads-down fanout walks ordered responder list for human-unmentioned messages; skips sender, non-verified panes, passed claims, and working claims >30s old. Fallback routes to remaining verified responders only. State-machine rows (considering/accepted/unavailable) deferred. |
| Settings CLI-version card | NMT feedback #E (@jstephenson), 2026-05-26 | CHANGE | Claude/Claude (claudev4) | New `GET /api/cli-version/latest` endpoint backed by an in-memory cache (1-hour TTL, stale-fallback on upstream failure) that fetches the latest published ant release from the GitHub Releases API. Settings System section now renders a `CliVersionCard` component with: "latest: X · YYYY-MM-DD" header, hint pointing operators at `ant --version` for their installed version (browser cannot see the local CLI binary), copy-to-clipboard buttons for `brew install jktfe/antchat/ant` + `brew upgrade jktfe/antchat/ant`, release-notes link. Hardcoded fallback commands shown when endpoint unreachable so operators are never stranded. Closes @jstephenson NMT feedback ("running CLI v3, needs clickable upgrade affordance"). 8 unit tests on parse + fetch + cache layers (parseVersionFromTag handles ant-v / v / raw shapes; fetchLatestFromGitHub throws cleanly on 4xx/5xx + missing tag_name; getLatestCliRelease caches within TTL + re-throws on cold-cache upstream failure). |
| Gantt summary text — honest about tracked actuals | speed-pact-v0 M-Demo honesty effort (lifted from speedykimi/m-demo-verification 032fc12), 2026-05-26 | CHANGE | Kimi/Speedyclaude | The `/plans/[planId]/gantt` page summary previously claimed "Bar position = task start/end" + referenced startedAtMs/endedAtMs. Bars actually render against tracked actual duration (`created → updated` timestamps), NOT predicted/scheduled time. Updated copy to be accurate. 1-line text fix lifted during JWPK branch-cleanup pass (msg_4n3l6x8xfp) — eliminates a misleading affordance description on a customer-facing surface. Original branch deleted as part of the cleanup; only the topic change kept. |
| Auto-register on `ant invite redeem` (F slice) | NMT feedback #F (@jstephenson via @james, hs9jv51zrh msg_sd5f3sw30s + msg_fpz9iyy4x1 + msg_s0kz6fncxa), 2026-05-26 | CHANGE | Claude/Claude (speedyclaude) | `ant invite redeem` and `ant invite join-url` now auto-register the calling tmux pane against the joined handle so PTY-inject delivers without a separate `ant register` call. New helper `scripts/ant-cli-redeem-autoregister.mjs` calls `POST /api/identity/register` with the caller's PID chain + `$TMUX_PANE` + agent_kind, then `POST /api/sessions/add` membership-mode to create the `room_memberships` row that `fanoutMessageToRoomTerminals` walks. Skipped when `--no-register` flag set OR when no tmux pane detectable (graceful fall-through to a hint pointing at the manual `ant register` command). Best-effort: a register/add-membership failure NEVER changes the redeem exit code or removes the tab-separated machine-readable success line (preserves script-consumer contract). Replaces James S's 5-min polling cron (job 68ec3708) — the workaround-as-tell that confirmed the gap was real enough to build polling-cron user-side. Diagnosis: `bindRoomHandleToLiveTerminal` (terminalHandleBinding.ts:37) returns null when no `terminal_records` row exists for the joining handle, so `room_memberships` is never seeded; fanout silently skips. 18 helper tests (truth table + flag overrides + 2-call orchestration + error containment) + 2 integration tests (--no-register skip path + happy-path with pane). svelte-check 0/0/0. New flags: `ant invite redeem [--name N] [--agent-kind K] [--pane P] [--no-register]` (and same on `ant invite join-url`). |
| `ant deck` root-init/create scaffold | JWPK msg_rsl5bdeiay + msg_etdzyzf6sx 2026-05-27 | DEFER | Codex/Claude | Superseded by the narrower "Normal deck build CLI" row above. `ant deck` intentionally ships only `build` and `list` for now: agents should not scaffold or install dependencies inside the ANT server repo as part of normal deck registration. Deck source creation remains a deck-folder concern, and normal room artefact registration remains `ant artefact add --kind deck --ref-url /d/SLUG`. |
| Deck roots Settings card + persistence layer | JWPK msg_etdzyzf6sx + msg_ocad1i11jg 2026-05-27 (build everything ASAP) | CHANGE | Claude/Claude (speedyclaude) | New `src/lib/server/deckSettingsStore.ts` persists user-editable deck root paths to `~/.ant/deck-settings.json`. Resolver `deckRootsResolved()` merges three layers (env var `ANT_BUILT_DECKS_ROOTS` → file layer → legacy fallbacks) deduped while preserving order. Both `/d/[slug]/+server.ts` and `/d/[slug]/[...path]/+server.ts` now use the central resolver — codex's original env-only `deckRoots()` consolidated. New API `GET/PUT /api/deck-settings` (admin-bearer gated) returns `{ envRoots, fileRoots, resolved }` for UI introspection and accepts `{ decksRoots: string[] }` for write. New `src/lib/components/DeckRootsCard.svelte` lands in `/settings/system` next to the CLI version card: shows env layer read-only (with hint pointing operators at shell rc), editable file layer (add row / remove row / save), effective resolved order numbered list. Spaces in paths preserved (validated with JWPK Dropbox path `/Users/jamesking/New Model Dropbox/James King/ANTdecks` round-trip test). Malformed file treated as empty (never strand operator). 14 tests on deckSettingsStore covering read/write/resolve/dedupe/spaces. svelte-check 0/0/0. |
| `ant deck export --as pptx` (best-effort .pptx) | JWPK msg_etdzyzf6sx 2026-05-27 (build everything ASAP) | CHANGE | Claude/Claude (speedyclaude) | New `scripts/ant-cli-deck-export.mjs` helper extracts text + image URLs from a built deck's `dist/index.html` (Reveal.js / Animotion convention) and writes a .pptx via pptxgenjs. Output: `<root>/<slug>/<slug>.pptx` alongside the source + dist. CLI: `ant deck export <slug> --as pptx [--root R]`. Honest scope: text + structure + image-URL references (not embedded). Animations / transitions / Svelte components / custom JS NOT exported — full visual fidelity stays with the built HTML at `/d/<slug>`. The pptx is for sharing-outside-ANT (give recipient a .pptx they can open in PowerPoint / Keynote / Slides). Helpers exported for unit testing: `stripHtml` (5-entity decode + tag strip + whitespace collapse); `parseDeckHtml` (depth-aware section scanner that flattens Reveal.js vertical-stack sections into leaf slides — naive non-greedy regex broke on nested sections because the outer `<section>` would consume the first inner `</section>`); `buildPptxFromSlides` (slide-by-slide pptxgenjs layout — title 28pt bold at top, body bullets, image-URL footnote if any). 13 tests on the helper + 11 on the CLI dispatch. svelte-check 0/0/0. pptxgenjs added as runtime dep (~3MB; lazy-imported inside the export verb so non-export CLI paths stay light). |
| `ant deck create/root-set/publish` + per-room overrides | JWPK msg_mom36j876y 2026-05-27 (both: per-project default folder + Cloudflare automation) | CHANGE | Claude/Claude (speedyclaude) | Three new `ant deck` verbs lighting up the per-room + sharing workflow. **`ant deck create <slug> --title T [--root R] [--room ROOM] [--bare]`** scaffolds a minimal Animotion-shaped folder (package.json + src/slides/100/slide.svelte + svelte/vite configs + deck-info.json metadata) under the chosen root; `--bare` writes only deck-info.json for operators using a different scaffolder. With `--room R`, reads the per-room override from `~/.ant/deck-settings.json roomOverrides` before falling back to first configured root. HTML-escapes hostile titles in the seed slide (regression-tested). **`ant deck root-set --room R --path P`** writes the per-room mapping; preserves existing `decksRoots` array + other room mappings on each call. **`ant deck publish <slug> --to cloudflare [--project-name N]`** shells out to `npx wrangler pages deploy <dist> --project-name <name>`, captures the deployed URL from wrangler stdout via regex, prints the ready-to-paste `ant artefact add --ref-url <URL>` command. Pre-flight: dist/ existence check + slug safety; ANT does NOT bundle wrangler (operator runs `wrangler login` once, brings their own CLI). `deckSettingsStore` extended with `roomOverrides: Record<string,string>` field — the Settings UI write path preserves overrides when only `decksRoots` is updated (so no UI write wipes the per-room map). `<DeckRootsCard>` shows the per-room map as a green-tinted sub-block when present. New API contract: `/api/deck-settings` GET response now includes `roomOverrides`; PUT accepts optional `roomOverrides` (undefined = preserve). 15 new tests on the CLI (create scaffold + bare + overwrite-refuse + slug safety + title required + HTML escape + room override + room fallback; root-set write + preserve-existing + dec`ksRoots-preservation + flag validation; publish target/slug/dist gates) + 6 new tests on store (roomOverrides round-trip + preserve + clear + non-object reject + empty-key filter + readRoomOverrides convenience). 59/59 deck-related tests + svelte-check 0/0/0. |
| /rooms delete + archive UI resync | JWPK msg_wdp0pvjgmo bug report, 2026-05-27 | CHANGE | Claude/Claude (claudev4) | The /rooms page kept showing rooms after archive (until manual refresh) and looked like delete "did nothing" entirely. Both server endpoints (`DELETE /api/chat-rooms/:id` softDeleteChatRoom + `POST /api/chat-rooms/:id/archive` archiveChatRoom) were succeeding, and RoomStrip was calling `invalidateAll()` correctly — but `/rooms/+page.svelte` had `let chatRoomsFromServer = $state(data.chatRoomsFromServer)` which reads the prop ONCE at construction. SvelteKit's invalidateAll updates `data.chatRoomsFromServer` but the local `$state` was a detached snapshot. The original code had a `svelte-ignore state_referenced_locally` comment suppressing the Svelte 5 warning that was specifically trying to surface this trap. Fixed by adding a `$effect` that mirrors the prop into local state on every update; the manual refreshRoomListFromServer path still works (whichever assignment runs last wins, and invalidateAll is canonical). Single-line root-cause fix on a user-visible bug — straight to main (rock-solid surface per the main/dev split). |
| Shared ANT-apps contracts: artefact summary + visibility priority + room prefs | eiw05zdurz msg_z0r3ys9xd5 + msg_q6g4v9jfgu 2026-05-27 (JWPK native-apps team formation; @antioscodex + @homebrewclaude shared contracts) | CHANGE | Claude/Claude (speedyclaude) | Three new endpoints serving both antios (iOS) + antchat (Mac native) per the team contract locked in eiw05zdurz. **`GET /api/artefacts/[artefactId]/summary`** returns `{ id, title, kind, refUrl, summary, createdBy, createdAtMs, updatedAtMs?, status?, renderTarget, stage }`. `renderTarget` is server-derived (NOT client URL-pattern guessing per @antioscodex critique): `kind=deck` artefacts whose refUrl resolves to a real `chat_room_decks` row → `in-app` + `stage.deckId` populated (Stage exception per JWPK msg_q6g4v9jfgu — voice/audio/path mutation iOS-critical). Everything else → `native-browser` (Animotion `/d/<slug>` decks, external URLs, all other kinds). `status` = `built`/`unbuilt` for Animotion decks (looks up `dist/index.html` across configured roots). Heavy Stage playback config (voice/audio/path schema) stays at a separate endpoint to keep summary sidebar-safe. **`/api/agents/visibility` extended** with `priorityScore` per room (formula: 100·openAsksForViewer + 0.1·population + 60M/msSinceLastActivity, muted=0) + per-viewer `pinned/muted/archived` flags merged from the new `room_member_preferences` table. Default sort: pinned-first → priorityScore desc. Defensive lastActivityAtMs parsing — falls back through ISO date parsing → creation timestamp → nowMs to never produce NaN downstream. `?includeArchived=1` query opts archived rooms into the response. **`GET/PUT /api/chat-rooms/[roomId]/preferences`** read/write the per-(room, viewer-handle) preferences (pinned/muted/archived). Partial update semantics — only flags supplied in PUT body are written. New schema: `room_member_preferences (room_id, handle, muted, pinned, archived, updated_at_ms)` with PK (room_id, handle). Deliberately no FK against chat_rooms (UI-state table, no orphan harm if room hard-deleted; keeps test setup light + survives room-id renames). New `priorityScoreFor()` exported for unit-testing the formula. **25 new tests** (15 visibility extension + 10 prefs store) including muted-zeroes-score, pinned-floats-above-score, archived-hidden-default, admin-without-handle skips prefs, NaN-safe edge cases, partial-update preservation, per-viewer/per-room independence. svelte-check 0/0/0. Mac-side @homebrewclaude + iOS-side @antioscodex both consume the same field set; layout choices stay local per the lane structure. Stage `/api/decks/:deckId/stage-config` endpoint is a follow-up slice for the heavy playback payload. |
| Reaction add/remove fan out via SSE | eiw05zdurz msg_v37be5ruo9 from @homebrewclaude (M1 reactions reconcile needs SSE), 2026-05-27 | CHANGE | Claude/Claude (speedyclaude) | `POST /api/chat-rooms/[roomId]/messages/[messageId]/reactions` now emits `{ type: "reaction_added", messageId, reaction }` via `broadcastToRoom` after a successful insert. `DELETE` emits `{ type: "reaction_removed", messageId, reactorHandle, emoji }` — only when something was actually removed (avoids noisy events for already-absent reactions). Both broadcasts wrapped in try/catch as best-effort decoration on the existing REST contract; HTTP response stays authoritative. Native apps (antchat M1 picker reconcile + future antios) can now subscribe to `/api/realtime/[roomId]/events` and apply optimistic-add → SSE-reconcile flow without polling. PTY-inject fanout to author terminal preserved unchanged. 23 existing reactions tests continue to pass. svelte-check 0/0/0. |
| Deck artefact review shell | JWPK msg_zyg0qdm6yn 2026-05-27 (verification interface as premium feature) | CHANGE | Codex | Normal deck artefacts now render in a review workspace on `/artefacts/:artefactId` instead of the generic legacy iframe shell. Raw `/d/<slug>` stays chrome-free for sharing, Cloudflare, and Zoom. The artefact page adds Fit / 16:9 / 4:3 frame modes plus a review-notes rail with clipboard capture, while existing validation controls stay attached to the artefact page. Stage presentations remain separate at `/decks/:deckId` for narration, live feedback, alternatives, and claim overlays. |
| Reactions viewerHasReacted on summary | eiw05zdurz msg_znoxuoppy8 from @homebrewclaude (M1 toggle bug — topReactors truncates at 5, clients infer wrong viewer state), 2026-05-27 | CHANGE | Claude/Claude (speedyclaude) | `MessageReactionSummary` now carries `viewerHasReacted: boolean`. `summariseReactionsForMessage(messageId, viewerHandles?)` accepts the FULL handle family from `requireChatRoomReadAccess` (not just the primary) so a reaction recorded under any of a viewer's aliases (e.g. `@jamesK`/`@you`/`@james` all belong to one user) counts as viewer-reacted. `withReactionSummaries` in messages GET threads `access.handles` through. Backwards-compatible — calling `summariseReactionsForMessage(id)` without handles yields `viewerHasReacted: false` for every emoji. SSE delta events stay reactor-pure (no per-connection branching) — clients reconcile their local summary copy by comparing the incoming `reactorHandle` to their own. 2 new tests on the summariser; 1 regression test on the endpoint (viewer reacts last when 5 others fill topReactors → API still reports viewerHasReacted=true). 21/21 reactions store + 38/46 endpoint tests pass (the 8 endpoint failures are pre-existing on main and unrelated). svelte-check baseline unchanged (8 errors all in docs/tauri-templates pre-existing). |
| `user.handleFamily` on /api/auth/me + /api/auth/login | eiw05zdurz msg_s21fibyq79 from @homebrewclaude + msg_wv1pzydu8b from @antioscodex (SSE reducer needs full alias set to match against incoming reactor handles), 2026-05-27 | CHANGE | Claude/Claude (speedyclaude) | `handlesForEmail` exported from `chatRoomReadGate` so the same alias-set computation that gates room reads also feeds client-side reducer matching. Both `/api/auth/login` and `/api/auth/me` responses now carry `user.handleFamily: string[]` (primary handle + any org-mapped historical handles + agent family expansion for alias-bound terminals). One field, populated once at login + every me-refresh — no extra fetch. Native app reactions reducer (M1 antchat + future iOS slice) compares incoming SSE `reactorHandle` against this family to flip viewer-owned summary fields. For fresh users with no org/terminal mapping, the family is just `[primary]`. Test fixture on login response updated to assert handleFamily presence. |
| V3 `/api/chat-rooms/:roomId/validation-summary` | JWPK orsz2321qb msg_zyg0qdm6yn (vital premium feature) → contract locked apps team eiw05zdurz msg_b6cnyrzdof, shipped 2026-05-27 | CHANGE | Claude/Claude (speedyclaude) | First-cut server endpoint for the V3 cross-app contract. Aggregates validation_runs (last 7d via new `listValidationRunsForArtefacts` helper) + pending validation verifier tasks (filtered by description-regex match) + computed overallTrustScore + server-owned trustState classification (incl. `stale` policy: lastCompletedAtMs > 7d). Returns the locked 9-field payload (defaultLensId / recentRunCount / pendingTaskCount / overallTrustScore / trustState / criticalGaps / sheetUrl / evidenceFormUrl / validationUxEnabled). Auth = `requireChatRoomReadAccess`. evidenceFormUrl matches caller handle family against pending task assignedTo; null for admin bearer (empty handles) and for callers without an assigned validation task. defaultLensId returns null (V5 territory). 9/9 endpoint tests + existing validationLensStore tests still green. |
| Verification bridge — lens v2 → executable PolicyBody | orsz msg_2v6i1v1ihs (@speedycodex split task ea380cff) + msg_y0vyfm9j1l (v2 shape) + msg_bge8evne1j (accepted), 2026-05-27 | NEW | Claude/Claude (speedyclaude) | New `src/lib/server/lensRulesBridge.ts` lowers the lens-designer authoring shape (v2: `{version, blocks: { kind: { mode: all\|any\|none, requirements: [{kind, count, specific?/allowedSources?/...}] }}, fallback}`) into the legacy `PolicyBody` consumed by existing `scoreValidationClaims` + `planValidationOrchestration`. Counts mapped (`agent→agents`, `person→humans` via AND/OR composition per `mode`, `file+filesystem→OR_agentsPlusFile`, `source/website/context_summary→OR_agentsPlusFile` alt-route). `mode: "none"` waiver creates an empty block (no requirement gates). Specific verifier handles + allowed domains + specific files + allowed sources surface separately via `extractAssignmentConstraints` for the orchestrator to gate assignment without affecting scoring counts. Public API: `parseLensRulesJson(raw)` (null on malformed, `{}` on legacy `[]`), `lensRulesToPolicyBody(rules)`, `extractAssignmentConstraints(rules)`, `rulesJsonToPolicyBody(raw)` convenience. 17/17 tests including integration round-trip through real scorer + orchestrator. Coordination: @speedycodex owns lens schema CRUD + web fallback UI; this bridge is the resolver they target. |
| Premium verification lens designer + CRUD | JWPK msg_0a1kzd041t + bridge split task 30d8d282, 2026-05-27 | CHANGE | Codex | Added premium-gated app-facing lens CRUD at `/api/verification/lenses` with V2 rules validation through `lensRulesBridge`, owner/public/org visibility, soft archive, and a readable audit trail. Added `/verification/lenses` as the web fallback authoring surface: power users can define claim block kinds, choose all/any/none verification mode, select verifier/source/file/website requirement rows, set counts and specific constraints, and inspect audit history. Native app UI remains primary; this route is the contract-backed fallback and server surface the apps consume. |
| `lensSchemaId` accepted on `/api/artefacts/:id/validate` | orsz msg_cf99778tsw — @speedycodex unblock after 8a8611d (lens designer CRUD + audit), 2026-05-27 | CHANGE | Claude/Claude (speedyclaude) | The artefact validation endpoint now accepts an alternative `lensSchemaId` payload field. When supplied, the executable PolicyBody is derived from the schema row stored by @speedycodex's `/api/verification/lenses` CRUD via `rulesJsonToPolicyBody` (the bridge from e8ca345). `policySlug` and `lensSchemaId` are mutually exclusive (400 when both). Visibility is enforced server-side: admin sees all, public is visible to everyone, user-scope requires the caller's handle family to include the schema's scope_id; org-scope returns 404 for non-admin until org-membership signal lands on `ChatRoomReadAccess`. Archived schemas, missing ids, and malformed `rules_json` return 404/400 respectively. 4 new tests (V2 lens resolves + both-supplied rejection + missing lens 404 + malformed rules 400) on top of the existing 9. The public-univer-demo gate stays slug-only — never fires for lens-schema-id paths. |
| docs/concepts/ant-stage.md — importable Stage concept doc | u5f11vr4rc msg_auy7bewdmq JWPK greenlight after the three-agents-missed-Stage symptom in u5f11vr4rc, 2026-05-27 | NEW | Claude/Claude (speedyclaude) | First file in `docs/concepts/`. Mirrors the canonical Stage framing (shell wraps any deck + 5 capabilities — narration / click-to-explain / live iteration / validation lens overlay / path mutation) into the repo so future agents can grep + import without relying on inherited memory banks. Frontmatter is memory-file-compatible (name + description + metadata.type/importable/category) so any agent can copy directly into their `~/.claude/projects/<...>/memory/` directory and treat it as a recallable memory. JWPK product-spec note in same thread (msg_q4lvkl8fz9): this points at a future server-side feature — memory search with user/org/public scope gating + bring-into-room attachment like skills/tools, so every agent in a room can see attached memories. That feature banked separately at `project_memory_search_and_attach_to_rooms_2026_05_27.md` in personal memory bank. Today's docs/concepts/ landing is the in-repo precursor + mitigation. |
| docs/concepts/_manifest.md — concept-doc pack manifest | u5f11vr4rc msg_yfbfeq63gy ("memories and jog on") + msg_5jtdgkgo6w (default-attached how-to-use-ANT memory pack), 2026-05-27 | NEW | Claude/Claude (speedyclaude) | Pack manifest for docs/concepts/. Lists every concept doc (shipped + planned task #31) with priority + per-room-kind default-attach policy (universal: rooms/asks/mentions/break; +plans+artefacts on plan-scoped; +stage+click-to-explain on stage-presentation; +verification+chair on premium-tier; +bridge+memory-and-attach on dev-substrate; +stage+verification+artefacts on apps-team). Locks the standard six-section structure for every concept doc (TL;DR / Capabilities / Architecture / CLI / Common patterns / Related) and the frontmatter contract for vault-portable memories. Becomes the spec for the future room-creation hook to read auto-attach rules from. |
| Stage substrate boundaries codified in ant-stage.md | u5f11vr4rc msg_9c5wle311d 2026-05-27 (answered in chat) + msg_kaknp0hjzy 2026-05-28 (audited as not-yet-shipped + committed), 2026-05-28 | CHANGE | Claude/Claude (speedyclaude) | Added "Substrate boundaries" section to `docs/concepts/ant-stage.md` covering the three architectural boundary questions raised by @enterprisecodex during yesterday's u5f11vr4rc session: (1) Stage source of truth — external for v1+v2, deck source stays in `ANT_BUILT_DECKS_ROOTS` filesystem, import-on-demand later; (2) Attached-memory grounding — attached-only by default, propose-attach for cross-scope, never silent grounding from private/org/public memories; (3) Mutation durability — three-layer model (base deck immutable / shell state mutable / session overlays ephemeral with durable session records via append-only event log) + explicit promote-to-deck-revision for accepted alternatives. Each boundary names the substrate (current + when-built) so downstream features respect the contracts. Closes the gap from yesterday's "answered in chat ≠ shipped" critique. |
| docs/concepts/ant-chair.md — Chair two-primitive split concept doc | u5f11vr4rc thread msg_jaisb3mmxb→msg_n5bm2h59ps (two-primitive split + cardinality + substrate enforcement) + orsz msg_kaknp0hjzy audit (chair concept currently invisible in repo) + eiw05zdurz msg_pwbn6ntjch (homebrew unblock request for jq17gcp9zw Chairs spec), 2026-05-28 | NEW | Claude/Claude (speedyclaude) | Second concept doc in the pack (after ant-stage.md). Canonical framing of the Chair two-primitive split: ANT Chair (user-scope proxy, max 1/user, NOT in rooms, has speech, user wallet pays) + Room Chair (room-scope operator, max 1/room, sits in room as member, has verbs, terminal owner pays). Two DB unique constraints + `chair.handoff(toMembershipId, reason)` atomic handoff verb. Translation discipline (plain-English user decisions, NOT technical jargon) banked verbatim from JWPK examples. Concrete Room Chair behaviours (move validation along, attach plans/artefacts, clean up, merge/dismiss asks, respond in-room) + proposed verb surface. CLI section flagged PROPOSED with substrate-not-yet-built status — honest disclosure that the spec is the contract, not shipped behaviour. Connection to verification interface (Room Chair drives validation-task progression via V3 endpoint + ANT Chair translates to user decision cards) made explicit. Frontmatter carries `status: spec-not-yet-built` for consumer clarity. Unblocks jq17gcp9zw Chairs spec room substantive design discussion. |
| Pane-binding supersession — fixes Vera-saw-xenoChat leak + subsumes PR #77 picker fix | JWPK msg_wlvguvfvqu/msg_8390722mjh antV4 2026-05-27 | CHANGE | Claude/Claude (claudev4) | New `superseded_at_ms` column on `terminal_records` + `supersedePriorRecordsForPane` helper. When createTerminalRecord or updateTerminalRecord claims a `tmux_target_pane`, any OTHER terminal_records on that pane are marked superseded. Six production readers updated to filter `superseded_at_ms IS NULL`: `listLinkedTerminalRowsForRoom` + `getLinkedTerminalRowBySessionId` + `isLinkedChatRoom` (linkedRoomTerminalLookup.ts — primary leak surface), `listLiveTerminalRecords` (new, also subsumes PR #77's archived-linked filter), `listAllPickableHandles` + `listKnownHandles` (the picker). Plus inbox readers: `humanInboxMembership` + `humanInboxBackfill` (prior agent should not retain inbox membership on a recycled pane). Plus `roomMembershipsStore` linked-room filter. `linkedRoomAgentGuffPurge` deliberately NOT filtered — cleanup paths still see superseded rows. `listTerminalRecords` (audit shape) deliberately NOT filtered — history preserved. 11 new tests (PS1-PS10) covering insert + update supersession, fanout/picker/linked-room/inbox exclusion, audit-shape preservation, self-supersession no-op, NULL-pane handling. svelte-check 3625/0/0. |
| Room-join preamble vault path resolved at emission time (not hardcoded) | JWPK orsz msg_szk0m5cwqn 2026-05-28 ("PULL that for them, saves a round trip; just make sure it isn't hardcoded in the repo") | CHANGE | Claude/Claude (speedyclaude) | Refactored `AGENT_JOIN_PREAMBLE_BODY` const → `buildAgentJoinPreamble(vaultPath?)` function. New `memoryVaultSettingsStore` mirrors `deckSettingsStore` pattern — settings file at `~/.ant/memory-vault.json` + env var override via `ANT_MEMORY_VAULT_PATH`. `resolveMemoryVaultPath()` reads at membership-creation time; preamble interpolates the actual path when set, prompts the agent to run `ant memory vault set` or set the env var when unset. No path is hardcoded in the repo — the resolver looks at config/env at runtime only. 23/23 tests pass (4 new env-var-path tests + 19 existing membership tests). |
| V2-server Slice 1 — verification_taxonomy + tag_lifecycle_events + 25-tag seed | JWPK eiw05zdurz 17-question ratification 2026-05-28 + deck d5024535-a495-45ea-9e4f-ba11bb197ab8 slide 10 substrate plan | NEW | Claude/Claude (speedyclaude) | Substrate foundation for V2-server reframe. New `verification_taxonomy` table with versioned tag definitions (id+version PK, lifecycle states proposed→active→deprecated→superseded→withdrawn, provenance system/org/user, scope_id for global vs org, protocol_resolver_json for static or conditional protocol classes incl. consensus-required 4th class, is_relational flag + family_root for tag families like source.supports-claim/source.refutes-claim, is_human_editable for jokes-flagged-ignorable use case). New `tag_lifecycle_events` table — append-only audit log capturing actor_handle + actor_kind + reason for every create/edit/deprecate/restore/supersede/classification_override/flag_ignorable event (audit-of-flagger is VITAL per JWPK ratification). New `verificationTaxonomyStore.ts` with createTag/editTag/deprecateTag/getLatestTagVersion/getTagVersion/listTaxonomy/listLifecycleEventsForTag/resetVerificationTaxonomyStoreForTests. Versioned-and-retained: editTag publishes a new version and KEEPS the old one (historical verifications resolve against their original tag definition). New `verificationTaxonomySeed.ts` seeds the 25 ANT default tags across 8 categories (claim/source/link/data/identity/content/context/process) + 2 relational tag families (source.supports-claim + source.refutes-claim — load-bearing for the default source-context-verification lens). Idempotent seeding (re-seed is safe). 16/16 tests pass. |
| V2-server Slice 2 — source_sets + source_set_members + source_set_audit | JWPK eiw05zdurz 17-question ratification 2026-05-28 (Q on source-set primitive replacing canned FCA/ONS seeds with a lens-creation skill) + ANT plan ant-verification-2026-05-28 milestone a6-source-sets | NEW | Claude/Claude (speedyclaude) | Source-set primitive — named groupings of trusted sources (domains/URLs/repos/file_collections/named_person/database/named_document_set) that lenses bind to. New `source_sets` table with `scope_kind` enum (`org-wide`/`lens-specific`), optional `bound_lens_id` for lens-specific sets, `approvers_json` for multi-party approval gates, `review_cadence_ms` for review reminders + `last_reviewed_at_ms`, lifecycle states proposed→active→deprecated→withdrawn (no hard-delete per substrate invariant). New `source_set_members` table with soft-delete via `removed_at_ms`/`removed_by`/`removed_reason` (preserves "we trusted X from D1 to D2" history); idempotent on (set_id, member_kind, member_value) for active rows + allows re-adding a previously-removed member as a new row. New `source_set_audit` table — append-only event log mirroring tag_lifecycle_events shape with event_kind ∈ create/rename/add_approver/remove_approver/deprecate/restore/review_checkpoint/add_member/remove_member (audit-of-flagger preserved). New `sourceSetsStore.ts` with createSourceSet/updateSourceSet/deprecateSourceSet/restoreSourceSet/recordReviewCheckpoint/getSourceSet/listSourceSets (filters: ownerOrg, boundLensId, lifecycleStates) + member ops (addMember/removeMember/listMembersForSet/findSetsContaining) + audit ops (listAuditForSet with newest-first + rowid tie-break for same-ms events). `findSetsContaining` is the lookup primitive verification flows use to ask "which set does this source belong to?" 21/21 tests pass. |
| V2-server Slice 3 — tagging_anchors + tag_applications + tagging_runs (Phase A3+A4) | JWPK eiw05zdurz 17-question ratification 2026-05-28 + ANT plan ant-verification-2026-05-28 milestones A3 + A4 | NEW | Claude/Claude (speedyclaude) | Tag-write substrate. New `tagging_anchors` table — content-type-agnostic anchor primitive that tag applications bind to. `content_kind` ∈ univer-block/markdown-offset/pdf-region/image-region/audio-timestamp/message-range/file-checksum; `anchor_data_json` is opaque to the substrate (adapter modules outside the store interpret per kind). `content_hash` on the anchor is the re-verification trigger: when an artefact's hash diverges, lenses with `re_verification_on_content_change=true` re-run affected applications. New `tag_applications` table — immutable "tag X applied to anchor Y by Z at T" records. Each carries (tag_id, tag_version) so historical applications resolve against their original tag definition (load-bearing for replayable audit). `target_claim_id` non-null only for relational tags (source.supports-claim.<claimId>); readers reconstruct the relationship from parameterised tag_id + claim_id. New `tagging_runs` table — grouping primitive. Each `ant tags apply` creates one run with `started_at_ms`; applications written during the run share `tagging_run_id`; `completeTaggingRun` sets `completed_at_ms` + final `application_count`. In-flight runs survive crash/abandon. New `tagApplicationsStore.ts` with anchor ops (createTaggingAnchor/getTaggingAnchor/listAnchorsForContent/listStaleAnchors), run ops (startTaggingRun/completeTaggingRun/getTaggingRun/listTaggingRuns with scope+initiator+in-flight filters), application ops (applyTag with fail-loud guards on missing anchor/missing run/completed run; getTagApplication; listApplicationsForAnchor; listApplicationsForRun; listApplicationsForClaim). 19/19 tests pass covering opaque adapter payloads, stale-anchor detection, run lifecycle + idempotent completion, fail-loud guard rails, replayable tag_version capture, multi-applicator co-tagging, relational target_claim_id, append-only invariant. |
| V2-server Slice 4 — tag_application_overrides (Phase A5) | JWPK eiw05zdurz 17-question ratification 2026-05-28 (audit-of-flagger VITAL + mandatory reason on overrides) + ANT plan ant-verification-2026-05-28 milestone A5 | NEW | Claude/Claude (speedyclaude) | Per-application classification overrides + flag-ignorable shape + withdraw verb. New `tag_application_overrides` table — append-only, every override is a new row, never a mutation. Three `override_kind` values: `classification` (changes the protocol class for this specific application, e.g. demote consensus-required → heuristic when context warrants; new_protocol_class required), `flag_ignorable` (marks application as ignorable for verification readers — handles "this is a joke claim, not a real one" / "this is an example, not an assertion"), `withdraw` (cancels the most recent override on the same application, revealing the prior override or original). Effective state is computed by walking the chain newest-first — `withdraw` rows pop subsequent non-withdraw rows. Stacked withdraws pop multiple. `reason` is REQUIRED on every row (whitespace-only rejected, surrounding whitespace trimmed). New `tagApplicationOverridesStore.ts` with recordTagApplicationOverride (full input validation: missing reason / missing newProtocolClass for classification / newProtocolClass on non-classification / missing tag_application), listOverridesForApplication (newest-first), getEffectiveOverride (chain resolver), listOverridesByHandler (audit view with limit + since options). 15/15 tests pass covering validation, classification, flag_ignorable, withdraw chain (1-deep + nested + stacked), append-only ordering, handler audit listing. |
| V2-server Slice 5 — verification_lenses rename + lens_tag_rows + temporal-rules columns (Phase A7 partial) | JWPK orsz path-(a) signoff msg_4bql3qh2em 2026-05-28 + ANT plan ant-verification-2026-05-28 milestone A7 | CHANGE | Claude/Claude (speedyclaude) | Pure-rename refactor of the live validation substrate to V2 vocabulary. `renameLegacyValidationTables()` migration runs before the DDL loop on every startup: `validation_schemas` → `verification_lenses`, `validation_runs` → `verification_observations`, `validation_schema_audit` → `verification_lens_audit`. Column rename: `schema_id` → `lens_id` on observations + audit (SQLite ALTER TABLE RENAME COLUMN). Idempotent — skips when only the new names exist; bails loudly if both names co-exist (manual reconciliation flag). All FK references auto-update under SQLite 3.25+ ALTER TABLE semantics; old indexes dropped + new ones added under the new naming. Added 3 new columns on `verification_lenses` per A7 spec: `minimum_pass_record_age_ms` (temporal rule — don't accept pass verdict until source record is older than N ms), `re_verification_on_content_change` (1=trigger re-run when anchor content_hash drifts), `out_of_scope_tags_json` (tags whose application excludes a fragment from the lens). Added new `lens_tag_rows` table with first-class per-tag rows (replaces the rules_json blob shape): (lens_id, tag_id, tag_version, expectation ∈ required/forbidden/consensus-required/heuristic-allowed/out-of-scope, min_verifier_count, verifier_mix_json, dispute_policy ∈ majority/unanimous/any-pass/any-fail/escalate, weight, notes). New `lensTagRowsStore.ts` with createLensTagRow (refuses orphan rows, sensible defaults) / listLensTagRows (per-lens, creation-order) / getLensTagRow / deleteLensTagRow / findLensTagRowsForTag (cross-lens audit). SQL identifiers swept across 8 consumer files: validationLensStore + lensRulesBridge + 6 API endpoint+test files. Public TS API (`ValidationSchema`, `schemaId`, store function names) deliberately unchanged for this slice — TS vocabulary refresh deferred to A10 V3 refactor. 115/115 verification-surface tests green (10 test files: existing 91 + 9 new lensTagRows + 15 tagAppOverrides). Migration tested on existing DB instances via the rename function's idempotency guards. |

| Room status pills + responder settings auth repair | JWPK orsz msg_iylhbup4xg + msg_q7v3tqfx80 2026-05-28 | CHANGE | Codex | Status pills now accept hook payloads where `session_id` is the CLI-own timeline id and `ant_session_id`/`terminal_id` is the ANT terminal whose pill should update. This fixes the observed mismatch where 19/19 hook sessions had no matching terminal row, so hook events were stored but never projected to room cards. Room cards now surface the activity pill in the room-name row next to the plan/live indicators, while last-update remains metadata below. Responder writes now use the shared `requireChatRoomMutationAuth` gate, so browser-session room members can add/move/remove responders without pidChain while CLI/agent callers still use pidChain and unauthenticated writes fail closed. Room mode switcher is surfaced on the room page for brainstorm/heads-down/closed. 28/28 focused route tests pass; svelte-check 0/0. |
| `ant chat send` shell-escape fix — --msg-file + --stdin + --msg-stdin flags | JWPK orsz msg_pkpow3hlfk 2026-05-28 (backtick + trailing-@ shell-escape issues) + msg_v6650efnby ("yeah worth it") | CHANGE | Claude/Claude (speedyclaude + parallel agent harmonized) | Add three new message-body input modes to `ant chat send` so message bodies containing shell-special characters (backticks for command substitution, `$` for variable expansion, `!` for zsh history, trailing `@`) post intact without requiring heredoc-via-tempfile workarounds. `--msg-file <path>` reads body from a file (no shell parsing of content). `--stdin` reads body from process stdin (pairs naturally with single-quoted heredoc: `ant chat send R --stdin <<'EOF'`). `--msg-stdin` kept as legacy alias for `--stdin`. New `resolveMessageBody()` helper enforces exactly-one-source semantics: refuses when no source is supplied AND refuses when multiple sources are supplied (no silent precedence). `--msg-file <path>` errors surface the cause path-prefixed (`--msg-file /tmp/x could not be read: ENOENT`). cli-manifest entry updated to document the three modes with explicit example using `--msg-file`. Two collaborating agents converged on the same primitive (S1 test from parallel agent uses `--stdin`; B1-B6 tests from this agent cover --msg/--msg-file/--msg-stdin paths + dual-source rejection + read-failure surfacing). 26/26 chat-send tests pass. The CLI binary needs rebuild to surface flags to live shell users; source-level + test-level coverage proves the dispatcher contract. |
| V2-server Slice 6 — verification_observations append-only refactor + verdictsStore (Phase A8) | JWPK eiw05zdurz 17-question ratification 2026-05-28 + ANT plan ant-verification-2026-05-28 milestone A8 | CHANGE | Claude/Claude (speedyclaude) | Append-only verdict event log over `verification_observations`. Each `recordVerdict()` INSERTs a new row; corrections never UPDATE. Effective verdict per `(lens_id, claim_anchor)` is the most-recent row (started_at_ms DESC + rowid tie-break) via `getEffectiveVerdict()`. Chain semantics: `parent_observation_id` links sequential observations; substrate refuses cross-claim parents (parent must match same lens+claim). New verdicts ratified per JWPK 17-question round: `dispute` (requires disputeReason; flagged), `insufficient_evidence` (result_json carries evidence-gathering trace), `retag_required` (signals tag staleness; lens can't evaluate). Legacy values (`pending`/`running`/`passed`/`failed`/`waived`) preserved. New columns on `verification_observations`: `parent_observation_id` (chain link FK to same table ON DELETE SET NULL), `verifier_handle` + `verifier_kind` (audit-of-flagger VITAL: `human`/`agent`/`system`/`automated`), `dispute_reason`. New `rebuildVerificationObservationsForAppendOnly()` migration uses the standard `_new + copy + drop + rename` pattern (SQLite refuses CHECK modification via ALTER); idempotent — skips when new columns already present. New `verificationVerdictsStore.ts` with recordVerdict (full validation: lens existence, parent existence + same-claim, dispute requires reason), getEffectiveVerdict (chain-newest), listVerdictChain (per-claim audit feed), listVerdictsByVerifier (per-verifier audit, capped 1000), listDisputesForLens (lens-owner needs-attention queue), getVerdict, resetVerificationVerdictsStoreForTests. 17/17 tests pass covering: terminal vs pending status timestamps, orphan-rejection, dispute-reason-required, chain isolation per lens/claim, parent validation (cross-claim refused), newest-first effective resolution, per-verifier listing with limit, dispute-only filter, append-only invariant (no UPDATE — repeated record creates new rows). |
| V2-server Slice 7a — POST/GET tagging-runs + verification-runs endpoints (Phase A9 CLI-facing pair) | JWPK eiw05zdurz lane assignment 2026-05-28 + ANT plan ant-verification-2026-05-28 milestone A9 | NEW | Claude/Claude (speedyclaude) | CLI-facing endpoint pair that consumes the substrate from Slices 3+4+6. **POST /api/scopes/[scopeId]/tagging-runs** — atomic-batch shape creates a tagging_run + applies N tags + auto-completes the run in one transaction; start-only shape returns the in-flight run for streaming applications via subsequent calls. Validates scope_kind (artefact/message/file/document/room), initiator_kind (human/agent/system), per-application required fields (tag_id, tag_version, target_anchor_id, applicator_handle, applicator_kind). applyTag failures surface 400 with the substrate's cause message preserved. **POST /api/scopes/[scopeId]/verification-runs** — records a verdict against verification_observations append-only log. Validates status (8 enums incl. dispute/insufficient_evidence/retag_required), verifier_kind (human/agent/system/automated). Refuses dispute without dispute_reason. Forwards parent_observation_id with same-claim check enforced by the store. **GET /api/scopes/[scopeId]/verification-runs?lens=&claim=** — returns `{ effective: VerificationVerdict | null, chain: VerificationVerdict[] }` for a (lens, claim) pair. Auth model: admin-bearer required on all three (substrate boundary until F1/F2 license-time org-namespace provisioning lands). scopeId param currently advisory; will become the org-namespace gate post-F1/F2. CLI consumers (`ant tags apply <scope>` + `ant verify <scope> --lens <lens>`) can wire these endpoints now. 16/16 endpoint tests pass: TR1-TR6 cover atomic-batch + start-only + 401/400 paths for tagging-runs; VR1-VR10 cover record + read + auth + validation + chain-claim safety + dispute-reason refusal for verification-runs. |
| V2-server Slice 7b — tags CRUD + lens_tag_rows endpoints (Phase A9 Author-UI pair) | JWPK eiw05zdurz lane assignment 2026-05-28 + ANT plan ant-verification-2026-05-28 milestone A9 | NEW | Claude/Claude (speedyclaude) | Author-UI-facing endpoint pair (counterpart to 7a CLI pair). New tag governance endpoints: **POST /api/tags** (admin-bearer; creates tag at version 1 with create lifecycle event; full validation: id/name/description/category required strings, provenance ∈ system/org/user, protocol_resolver object, author_handle + author_kind required), **GET /api/tags** (open read; filters: provenance, scope_id, category, lifecycle, include_history); **GET /api/tags/[tagId]** (open read; supports ?version=N for historical lookups — replayable audit substrate); **POST /api/tags/[tagId]/deprecate** (admin-bearer; deprecates or supersedes via replacement_tag_id with lifecycle event); **GET /api/tags/[tagId]/audit** (open read; full chain newest-first). New lens-tag-rows endpoints: **POST /api/verification/lenses/[lensId]/tag-rows** (admin-bearer; full validation: expectation ∈ 5 enums, dispute_policy ∈ 5 enums, verifier_mix array-of-strings, weight finite number, min_verifier_count positive integer; refuses orphan rows via store's lens-existence check, surfaces as 404); **GET /api/verification/lenses/[lensId]/tag-rows** (open read; lists per-lens rows creation-ordered); **DELETE /api/verification/lenses/[lensId]/tag-rows/[rowId]** + **GET .../[rowId]** (admin-bearer DELETE; defense-in-depth lens-id+row-id cross-check returns 404 on path mismatch). Auth boundary: admin-bearer for writes (F1/F2 will surface org-admin gating); reads open per substrate trust-surface model (lens authoring + tag definitions are part of the trust signal so consumers can resolve historical applications). 23/23 endpoint tests pass: T1-T13 cover tag POST/GET/single-GET/deprecate/audit + supersession path; LTR1-LTR10 cover lens-tag-row POST defaults/full-config/orphan-404/auth/list/delete/path-mismatch-404. A9 substrate-endpoint surface now COMPLETE (CLI pair 7a + Author CRUD 7b). |
| V2-server Slice 8 — A10 V3 validation-summary derivation refactor under new substrate | JWPK eiw05zdurz V3 contract lock 2026-05-28 + ANT plan ant-verification-2026-05-28 milestone A10 + Phase D unblock dependency | CHANGE | Claude/Claude (speedyclaude) | Refactor V3 validation-summary derivation to honor the new append-only substrate from Slice 6 while PRESERVING the locked 9-field payload shape per the contract commitment. Locked fields unchanged: `defaultLensId`, `recentRunCount`, `pendingTaskCount`, `overallTrustScore`, `trustState`, `criticalGaps`, `sheetUrl`, `evidenceFormUrl`, `validationUxEnabled`. New derivations: (a) `overallTrustScore` now EXCLUDES rows with dispute/insufficient_evidence/retag_required statuses — these are not signal for the trust score and contaminated the avg under the legacy code path; (b) `criticalGaps` extended to surface dispute (kind=`disputed-verdict`) + retag_required (kind=`retag-required`) alongside the existing `failed-validation` rows — clients render trustState for colour but criticalGaps[].kind drives "what's wrong" narration; (c) `trustState` pending signal now includes insufficient_evidence + retag_required + pending + running rows in `inFlightCount` so the chip shows 'pending' instead of 'unknown' when verifiers have started but not converged. CriticalGapKind union widened: `'failed-validation' \| 'disputed-verdict' \| 'retag-required'` (additive — existing 'failed-validation' consumers unaffected). 13/13 tests pass: 9 original payload/score/trustState/stale/pending/auth covered + 4 A10-specific: dispute-surface + score-exclusion, retag-required surface, insufficient_evidence pending-signal, payload-shape-locked (exact key-set check guards against future drift). Phase A substrate COMPLETE — A1 through A10 shipped. Phase D (iOS Trust chip @antiosclaude) unblocked. |
| V2-server Slice 9 — three default lens scaffolds (Phase B4) | ANT plan ant-verification-2026-05-28 milestone B4 + JWPK 17-question ratification (canned FCA/ONS lenses removed; default scaffolds reference real lens schema) | NEW | Claude/Claude (speedyclaude) | Phase B4 default lens scaffolds — three canonical lenses seeded at server bootstrap that reference the substrate (verification_lenses + lens_tag_rows + Slice 1 default tags). NOT canned regulatory lenses (per JWPK direction during ratification — orgs build their own via B1-B3 lens-creation skill); these are SCAFFOLDS showing how lens_tag_rows compose for common verification shapes. **Lens 1: lens-link-verify-1-agent** (cheap link checking) — 3 link rows (link.html / link.file / link.repo) at min_verifier_count=1 + any-pass dispute policy. Suitable for internal documents. **Lens 2: lens-link-verify-2-agent** (high-stakes link consensus) — same 3 link rows at consensus-required + min_verifier_count=2 + majority dispute policy + process.flagged-ignorable out-of-scope to exclude jokes/examples. For regulatory filings + public marketing. **Lens 3: lens-source-context-1h1a** (claim-heavy content) — investment_memo kind; 8 rows binding claim.factual (1h+1a, unanimous), source.supports-claim (required), source.primary (weight 2.0, heuristic-allowed), source.reputable (weight 1.5), source.refutes-claim (consensus-required, escalate), source.unverified (forbidden), content.direct-quote (1h+1a, unanimous), process.flagged-ignorable (out-of-scope). New `verificationLensSeed.ts` with seedDefaultLenses (idempotent — skips existing) + getDefaultLensIds. New `verificationLensSeed.test.ts` with 6/6 tests: full seed-and-verify, idempotency, per-lens shape verification (link-1-agent / link-2-agent / source-context-1h1a), notes-rationale presence. Closes B4 in plan. Substrate is functionally complete for downstream Phase C/D consumers — they can pick a default lens or author their own via B1-B3 skill. |
| `ant chat reply <messageId>` room-derived replies | JWPK orsz msg_6yjcdpyxl6 + msg_l0rhzd6j95 + msg_rs0gos2daf 2026-05-28 (wrong-room posts, missing reply edges, remote human broadcast intent) | NEW | Codex | Adds the first zero-token message-intent guardrail: a top-level `ant chat reply <messageId>` command. The CLI looks up the parent message via `GET /api/chat-rooms/messages/:messageId`, derives the target room from the persisted message row, then posts through the existing room message endpoint with `parentMessageId` set. This removes the need for agents to copy room ids when answering a specific message. New message lookup route applies the existing room read gate after loading the parent row, so private room contents are not exposed by message-id lookup. Legacy `ant chat <roomName> reply <messageId>` remains intact. Tests cover CLI room derivation + endpoint 200/401/404. Follow-up remains deterministic broadcast/reply warnings and room-scoped mention guards. |
| Reply-shaped broadcast guard for `ant chat send` | JWPK orsz msg_seywrarz3m + msg_6yjcdpyxl6 2026-05-28 (agents posting replies as broadcasts) | CHANGE | Codex | Adds the second zero-token message-intent guardrail. `ant chat send` now rejects obvious reply-shaped broadcasts before any HTTP request when the body contains an ANT message id (`msg_...` or `reply-to=msg_...`) but the caller did not set `--parent-message`. The error points agents to `ant chat reply <messageId> --stdin`, `--parent-message`, or explicit `--broadcast-ok` for intentional status broadcasts that cite a message id. `--broadcast-ok` is a boolean CLI flag and is documented in the manifest. Tests cover reject-before-fetch, explicit broadcast override, and explicit parent-message passthrough. |
| V2-server Slice 10 — B2 prompt template + parser + skill scaffold | JWPK eiw05zdurz msg_s1q1otis70 2026-05-28 ratify (Q1=a/Q2=b/Q3=a) + msg_6i0y40vbxc "keep pushing" + ANT plan ant-verification-2026-05-28 milestone B2 | NEW | Claude/Claude (speedyclaude) | Phase B2 substrate-shaped pieces. **Prompt template**: docs/specs/create-verification-lens-prompt-template.md — actual LLM prompt (system message + user message templating) honouring the three JWPK-ratified rules (Q1 active+org-scope-only tags catalog / Q2 surface-suggested-not-auto-created source-sets / Q3 explicit-error_kind refusal with 5 error_kind enum values). Output contract specified verbatim with 8 parser invariants documented. **Skill module** `src/lib/server/createVerificationLensSkill.ts` ships: typed SkillInput/SkillSuccessOutput/SkillRefusalOutput shapes (12 type unions including LensTagExpectation/LensTagDisputePolicy/LensKind/SourceSetMemberKind/SkillErrorKind), input validator (requirements 50-4000 chars + lens_name alphanumeric 3-80 + framework_hint cap), SYSTEM_MESSAGE constant + buildUserMessage(input, tagsCatalog, availableSourceSets), parseSkillResponse with 8 invariants enforced (JSON parse / kind enum / lens.name/description/kind / re_verification boolean / tag_id in catalog / expectation enum / dispute_policy enum / source_set_binding resolution / refusal error_kind enum / refusal reason non-empty), runCreateVerificationLensSkill entry function with injectable CallModel parameter + early refusal on regulated-framework-with-zero-sources (saves a model call on known-no-go). **NOT shipped here (deferred to B3)**: actual LLM client (Anthropic SDK / Bedrock wiring), model selection (Sonnet/Haiku/Opus), prompt caching (cache_control), skill_invocations audit table + ingest endpoint, 3 example outputs for JWPK lock review. The substrate is testable end-to-end with stub callModel; B3 plugs in the real model call + persistence. 17/17 tests pass covering: input validation (3 paths), Q2 ratified early-refusal short-circuit, parser success (with markdown-fence tolerance), parser refusals for parse_error/unknown_tag/invalid_enum/unknown_source_set/refusal-passthrough/invalid_refusal, prompt construction invariants, end-to-end with stub callModel, callModel-error surfacing as parse_error refusal. |
| V2-server Slice 11 — skill_invocations audit substrate (Phase B3 partial) | JWPK msg_6i0y40vbxc 2026-05-28 "keep pushing" + ANT plan ant-verification-2026-05-28 milestone B3 + B1 spec table reservation | NEW | Claude/Claude (speedyclaude) | Phase B3 audit substrate — the half of B3 that doesn't need JWPK model/cost ratify. New `skill_invocations` table (append-only): id + skill_id + invoker_handle + invoker_kind (human/agent/system) + scope_id + input_requirements_hash (SHA-256 for dedup) + input_json + output_json + output_lens_id (FK to verification_lenses ON DELETE SET NULL; null on refusal) + error_kind (null on success; set on refusal) + model_used + cost_estimate_usd + invoked_at_ms. Four indexes: scope+time / invoker+time / output_lens (partial WHERE NOT NULL) / skill+time. New `skillInvocationsStore.ts` with recordSkillInvocation (auto-hashes requirements + writes), getSkillInvocation, listSkillInvocations (filters: scopeId/invokerHandle/skillId/since/limit; newest-first), getScopeCostSummary (invocationCount + refusalCount + totalCostUsd for F2 cost-cap gate), findMostRecentForHash (dedup lookup — "looks identical to your last call X minutes ago" UX), resetSkillInvocationsStoreForTests. 15/15 tests pass covering: full-field persistence with FK to a real lens, refusal-with-null-lens-id-and-error_kind, hash distinctness across requirements text, filter combinations (scope / invoker / since / limit), newest-first ordering, cost summary aggregation incl. refusal counting, hash lookup scope-isolation, get-by-id null + present paths. **NOT shipped here (still needs JWPK ratify before B3 closes)**: actual LLM client wiring (Anthropic SDK / Bedrock pick), model selection (Sonnet/Haiku/Opus), cost cap value per scope per month, 3 example outputs for JWPK lock review per plan acceptance "JWPK reviews 1 example output before lock". The substrate is now ready for the LLM endpoint slice — it just needs to call runCreateVerificationLensSkill (Slice 10) + recordSkillInvocation (this slice) inside one transaction with the org-admin auth gate. |
| Agent reply instruction uses room-derived reply command | JWPK orsz msg_y4j7l2x4na 2026-05-28 (agents still posting in wrong room) | CHANGE | Codex | Updates PTY injection envelopes so the default instruction no longer tells agents to copy a room id into `ant chat send ROOM_ID --msg`. Single-message and single-room batch envelopes now instruct `ant chat reply <messageId> --stdin`, using the source/latest message id so the CLI derives the room from the persisted parent message. Cross-room batch envelopes now say `ant chat reply MESSAGE_ID --stdin`, preserving a message-id decision rather than a room-id copy. Reaction fanout direct injections now use `ant chat reply <messageId> --stdin` too. This builds on `baf7e1e` and removes the old unsafe command shape from the normal agent prompt path. Focused envelope tests pass; the broader fanout suite still has the known unrelated read-receipt SSE assertion failure. |
| GET /api/scopes/[scopeId]/tag-applications — D2 unblock | @antiosclaude eiw05zdurz msg_fpbwjn57wv 2026-05-28 ("waiting on GET tag-applications shape") + Phase D2 iOS tag overlay dependency | NEW | Claude/Claude (speedyclaude) | Read endpoint surfacing tag_applications for one of three filter dimensions: ?anchor=<anchorId> (typical tag overlay use — show all tags applied to a fragment), ?claim=<claimId> (relational tags targeting a claim — source.supports-claim/refutes-claim render), ?run=<taggingRunId> (audit feed — show all applications written by one ant tags apply invocation). Exactly-one-filter contract enforced (multi-filter combined scans return 400; no-filter returns 400 with the explicit usage message). scopeId currently advisory (becomes org-namespace gate post-F1/F2). Open read per the substrate trust-surface model — tag applications are part of the verification audit trail visible without auth, same as tag definitions + lens authoring shape. Unblocks D2 iOS tag overlay (@antiosclaude) + M12 right-click flag (Mac) + Verification Tags page audit feed. 7/7 tests pass: anchor / run / claim filter dimensions + no-filter 400 + multi-filter 400 + open-read no-auth + empty-result 200. |
| V2-server Slice 12 — PULL-not-PUSH cleanup + skill registry endpoints + skill-invocations audit endpoint + vault mirror | JWPK eiw05zdurz msg_pgp1n75ufb + msg_o1e307juug 2026-05-28 (architecture reframe: ANT doesn't call models; agents pull skill from memory and execute; pages provide user-driven write path) + msg_91e8y2wlm4 ("validation memory will need to be a core memory in the memory-pack") | CHANGE | Claude/Claude (speedyclaude) | Cleanup + new endpoints implementing the PULL-not-PUSH architecture. **Removed**: `runCreateVerificationLensSkill(input, callModel)` orchestrator from src/lib/server/createVerificationLensSkill.ts — wrong abstraction (ANT doesn't call LLMs). The skill module retains only the types (SkillInput / SkillSuccessOutput / SkillRefusalOutput), SYSTEM_MESSAGE constant, buildUserMessage helper, and parseSkillResponse validator — these serve both agent-acts and user-acts-via-page paths. 10/10 retained tests pass. **New endpoint** `GET /api/skills/[skillId]/protocol`: agent-facing skill protocol fetch. Reads protocol files from docs/specs/ and returns { skill_id, description, files: [{path, content}] }. Open read per substrate trust-surface model. SKILL_REGISTRY map declares which files compose each skill (currently create-verification-lens → spec + prompt-template). 2/2 tests pass. **Updated** static/skills.json to register create-verification-lens in the discovery manifest at GET /api/skills. **New endpoint** `POST/GET /api/skill-invocations`: append-only audit log. POST writes a skill_invocations row (admin-bearer; agents + page surfaces both POST when they execute a skill). GET filters by scope/invoker/skill/since/limit (open read). 7/7 tests pass. **Core memory mirror**: copied docs/specs/create-verification-lens-skill.md + docs/specs/create-verification-lens-prompt-template.md to memory-pack/skills/ so agents PULL the protocol from their vault per the architecture (JWPK direction). Memory-pack README will reference the skills/ subdir as core. **Plan-decision alignment**: @antioscodex landed plan decision in ant-verification-2026-05-28 making validation terminal/page driven (not model-called); acceptance gates on B2/B3/C3/D2/G2 reject model-call framing. This slice implements that decision in code. 41/41 tests pass across all modified surfaces (skill module + skill_invocations store + protocol endpoint + skill-invocations endpoint + tag-applications endpoint). |
| V2-server Slice 13 — D3/D4/D5 substrate-gap endpoints (PUT tag/version + POST protocol-override + GET audit) | @antiosclaude eiw05zdurz msg_9mor22l0lt 2026-05-28 (sub-agent swarm shipped D3/D4/D5 iOS; surfaced 3 server-side endpoint gaps) | NEW | Claude/Claude (speedyclaude) | Three thin HTTP route wrappers around stores already shipped in Slices 1, 4, and 5. Unblocks iOS D3 Author wizard + D4 per-application override + D5 audit feed surfaces. **PUT /api/tags/[tagId]/version** — publishes new tag version via editTag(); old version retained for replayable audit. Body: name?/description?/protocol_resolver?/is_human_editable?/actor_handle/actor_kind?/reason? Returns the new version row. 5/5 tests. **POST /api/tag-applications/[applicationId]/protocol-override** — records classification/flag_ignorable/withdraw override via recordTagApplicationOverride(). Body: override_kind, new_protocol_class (for classification), handler_handle, handler_kind, reason (REQUIRED non-empty per audit-of-flagger). 8/8 tests covering all 3 kinds + missing/invalid path. **GET /api/tag-applications/[applicationId]/audit** — returns the override chain newest-first + computed effective state via listOverridesForApplication + getEffectiveOverride. Open read per substrate trust-surface model. Tests share the protocol-override test file. Auth model: POST/PUT admin-bearer; GET open. 13/13 tests pass overall. Substrate truth verified: stores existed since Slice 1/4/5; only the HTTP surface was missing. |
| V2-server Slice 14 — F1 orgs/admins substrate + F2 premium-tier author gates + G1 research consolidation | JWPK eiw05zdurz msg_i1f1rk6pbw 2026-05-28 "spin up swarms, get to 100%" + plan milestones f1-org-namespace + f2-premium-gates + g1-concept-doc | NEW | Claude/Claude (speedyclaude — 3-agent swarm: F1/F2/G1 sub-agents) | Triple-slice ship via parallel sub-agents. **F1 substrate**: new `orgs` table (id + display_name + namespace_prefix UNIQUE + tier ∈ oss/premium/enterprise + created_by + created_at_ms + archived_at_ms) + `org_admins` table (with soft-revoke via revoked_at_ms + revoked_by audit-of-flagger). New `orgsStore.ts` with createOrg / getOrg / getOrgByNamespacePrefix / listOrgs / setOrgTier / assignOrgAdmin / revokeOrgAdmin / listOrgAdmins / isOrgAdmin / resetOrgsStoreForTests. 4 endpoints: POST /api/orgs (admin-bearer; 409 on duplicate id/namespace), GET /api/orgs + /api/orgs/[id] (open read), POST /api/orgs/[id]/admins (assign), DELETE /api/orgs/[id]/admins/[handle] (soft-revoke via x-revoked-by header). 39 tests (17 store + 22 endpoint). **F2 server-authoritative gates**: new `verification_author` feature flag (`tier !== 'oss'`) + `requireVerificationAuthorTier()` helper in featureGates.ts. Wired into 7 Author endpoints (POST tags + PUT tag/version + POST tag/deprecate + POST verification/lenses + POST lens/tag-rows + DELETE lens/tag-rows/[row] + POST skill-invocations). OSS tier returns 403 with upgrade-CTA copy; premium passes. Read paths + apply + run-lens + audit stay open per spec. Tests updated with hoisted `featureGateState` mock pattern for OSS-403 paths (6 new tests across 5 existing test files). **G1 concept doc consolidation**: new canonical doc at `~/CascadeProjects/ObsidiANT/memory-pack/research/verification-classification-system-canonical-2026-05-28.md` (222 lines, 23.7KB). Absorbs three identified input drafts (speedyclaude / homebrewclaude / antioscodex; fourth slot occupied by pre-reframe spec). Includes ratified architecture + 17-question table + primitive→table→SHA reference + delta between input proposals and shipped substrate + read-first pointers. Updated memory-pack/README.md index + docs/concepts/_manifest.md + SUPERSEDED headers on the three input draft stubs. 84/84 tests pass across all affected surfaces. After this slice: 26/33 plan milestones done (12 mine + 13 other agents); only C1-C5 + E1 + E2 remain. |
