---
title: ANT v0.2 — Regression Corpus Index
date: 2026-05-29
authors: ["@cv4", "@speedyc"]
status: skeleton — bodies stay todo() until each v0.2 schema PR lands
companion: scripts/v0.2-regression.test.ts
concept: docs/concepts/ant-v02-identity-and-recovery.md
---

# ANT v0.2 Regression Corpus — Index

Human-readable index of the 9 cases that gate the v0.2 schema cut-over. Every
case is paired with an `it.todo()` stub in `scripts/v0.2-regression.test.ts`.
Bodies fill in incrementally as each migration PR lands — never up-front, to
avoid asserting against a schema that isn't there yet.

## How to read this table

| Column | Meaning |
|---|---|
| `#` | Case number — matches the stub order in the test file. |
| `Title` | Short summary of the failure mode. |
| `Source incident` | Date + room + msg_id that produced the case, plus a one-sentence description. |
| `v0.2 invariant` | The schema property that makes the failure structurally impossible (citing the concept doc). |
| `Status` | `todo` (stub only) / `asserted` (body written, awaits v0.2) / `passing` (running green against v0.2). |

## Status board

| # | Title | Source incident | v0.2 invariant | Status |
|---|---|---|---|---|
| 1 | Locale-format pid_start mismatch | 2026-05-29 AM silence forensic (19 agents) + PM @cv4 fresh-start trip — exact-string equality on DB-stored locale string vs caller-supplied locale string returned null, surfacing as 403 "Server-resolved identity required". | `runtimes.pid_start_iso` (ISO 8601 UTC) — no locale on the wire (concept doc §Identity Layer). PR-A normalisation already shipped against current schema. | todo |
| 2 | Shadow-terminal shadowing in pidChain walk | 2026-05-29 AM — stale `claudev4-postrestart` row had pid=51382 (live) shadowing the canonical `claudev4` row, so pidChain resolution returned the wrong identity. | `UNIQUE INDEX (agent_id) WHERE status='live'` on `runtimes` (concept doc §Three Structural Invariants #1) + lookup filters on `status='live'`. | todo |
| 3 | Dual-bind on fresh register (UI roster vs fanout drift) | 2026-05-29 PM @speedyc trip in v4.1 room qexiaw2xpg — `chat_room_members` showed @speedyc present while `room_memberships` pointed at stale dea7fdf0; fresh terminal t_vjly79fxu9 sat idle. | SINGLE `memberships` table (no roster/fanout split) + fanout target DERIVED from `agents.current_runtime_id` at send time, never cached (concept doc §Room Layer + §Three Structural Invariants #3). | todo |
| 4 | Six-rooms × stub-id breakage | 2026-05-29 AM bulk fanout rebind across 33 stale bindings affecting 19 agents in 6+ rooms; concurrent rebinds collided on the same `room_memberships.id`. | Derived fanout (no cached column to race on); rebinds write `audit_events` rows, not row UPDATEs (concept doc §Three Structural Invariants #3 + §Forensic Layer). | todo |
| 5 | Competing-rebind race (instance #4 of 2026-05-29) | 2026-05-29 PM @speedyc msg_r4xqwhayvq — "@cv4 fixing me broke @codex4 temporarily"; UPDATE on terminals.pid_start during concurrent agentStatusPoller read returned NULL until codex4 re-registered. | Derived runtime resolution + atomic swap on `agents.current_runtime_id` (concept doc §The TigerResearch Recovery Flow step 7). State transitions via INSERT into audit_events + runtimes, never row UPDATE. | todo |
| 6 | Fleet-restart auto-reclaim | JWPK msg_rj7xtj7krk — "I might need to restart the server... all the panes will die". Today: no batch recovery; each agent must individually re-register and hope no binding drifts. | `reclaim_requests` primitive + `ant admin reclaim --all-stale --auto-approve` iterates stale agents and auto-approves on signed challenge from new runtime (concept doc §The TigerResearch Recovery Flow — Server restart variant). | todo |
| 7 | Peer-driven upgrade reclaim | JWPK msg_rj7xtj7krk — "go and brew upgrade claude in another terminal, one of you exits the others, then resumes you either in the same pane or another". Today: no peer-authorised reclaim path. | `reclaim_requests` row with `requesting_agent_id != agent_id`; super-admin or peer-with-tool-grant authorises (concept doc §The TigerResearch Recovery Flow — Peer-driven upgrade variant). | todo |
| 8 | Nifty-orphan-grant (deleted skill still loading) | JWPK msg_mjh7rgi3wa — "Using nifty. Where did that come from? That was a memory that I was supposed to have deleted." Filesystem-glob skill loading leaves ghost capabilities. | `tools` row is single source of truth; soft-delete invalidates every `tool_grants` row referencing it; `ant audit orphans` surfaces leaks (concept doc §Tool Catalog (the "nifty" Incident Fix)). | todo |
| 9 | Multi-key survives device-revoke (James Stevenson car crash) | JWPK msg_gtzwsh340p — "James Stevenson was in a car crash on Wednesday, and his laptop got fucking stuck in the boot... What happens then?" Single trust_pubkey = lose private key = lose agent forever. | `agent_trust_keys` table allows N keys per agent (`key_kind in {device, recovery, hardware, passkey}`); revoking one key leaves others able to sign (concept doc §The Key-Loss Recovery Story). | todo |

## Lifecycle of a row in this table

1. **Stub lands** (this PR) — `it.todo()` in the test file, `Status: todo` here. CI passes (pending tests are not failures).
2. **Assertion lands** — concurrent with the migration PR that introduces the relevant v0.2 table/column. `it.todo()` becomes a real `it(...)` body. `Status: asserted`. Test fails against v0.1 schema; that's expected and the test is skipped under a `describe.skipIf(!v0_2_schema_present)` guard until cut-over.
3. **Cut-over** — the schema flip flips the guard. `Status: passing`. The corpus is now a live regression gate.

## Permanent CI gate proposal

JWPK ratification pending. Proposal: every schema-touching PR runs this corpus
as a required check after v0.2 cut-over. Rationale: the failure modes captured
here are correlated — a single careless migration could quietly reintroduce
the `(agent_id) WHERE status='live'` partial-index assumption, for example —
so we want continuous coverage, not a one-shot gate.

See concept doc §Open Questions for JWPK #1.
